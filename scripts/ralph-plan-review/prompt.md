# RALPH Plan Review — Per-iteration prompt (amp variant)

This file mirrors `CLAUDE.md` so `ralph.sh --tool amp` works. The Claude Code path reads `CLAUDE.md` directly. See `CLAUDE.md` for the canonical instructions.
