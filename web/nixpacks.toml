# caddy is NOT listed here: Nixpacks auto-provisions it from the `caddy run` start
# command below, and declaring it again collides (duplicate _caddy completion → nix-env
# "Unable to build profile" conflict on the Railway builder).
# nodejs_22: several transitive deps require node >=20.19 || >=22.12; nixpacks
# nodejs_20 pins 20.18.1 (too old → EBADENGINE). 22 also matches the lockfile regen env.
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "caddy run --config Caddyfile --adapter caddyfile"
