# Lane manifest — created by /gabe-lane or migrated by /gabe-init
name: p1-backend
phase: p1                 # e.g., p1, p2, b0 — null for default/legacy/stream-only lanes
stream: backend           # e.g., backend, ux, observability — null for default/legacy lanes
stream_only: false        # true when lane spans phases or has no phase anchor (e.g., ux-mockups)
branch: gabe/p1-backend   # gabe/<lane-name> for lane branches; main for default/legacy
created: 2026-04-23       # YYYY-MM-DD
migrated_from: null       # "legacy" on init migration, null on fresh create
dep_asserted: null        # user-provided rationale when --ignore-deps bypasses blocked check
merged_at: null           # YYYY-MM-DD set by /gabe-lane --merge; null while active
archived_at: null         # YYYY-MM-DD set by /gabe-lane --archive; null while live
