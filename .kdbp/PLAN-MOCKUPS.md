# Parallel Plan — React + Storybook Mockups (design-lab)

<!-- status: active -->
<!-- project_type: mockup -->
<!-- parallel_to: .kdbp/PLAN.md (P16 Compliance — worked in a separate session; NEVER edit PLAN.md from this lane) -->

## Goal

Design the gastify product surface — desktop, tablet, and mobile views — as production-grade React + Storybook mockups in a fresh workspace (`design-lab/`), the same fashion and artifact set as the Gustify project. The artifacts later drive (a) the `web/` interface and (b) the Android (`mobile/`) app.

## Context

- **Created:** 2026-06-10. User direction: fresh mockup workspace (not `web/`, not reviving `frontend/`); parallel plan because P16 (compliance/launch) is mid-flight in another session; IA decided during design via comparison spikes.
- **Surfaces today:** `web/` = live prod web (React 19 + Vite 8 + Tailwind 4, tokens in `web/src/styles/global.css`, NO Storybook); `mobile/` = live Android (Expo 55, hardcoded hex, no tokens); `frontend/` = frozen BoletApp port carrying the only Storybook (~90 stories) — architecturally divorced from the shipped product. The `docs/rebuild/ux/` contracts still point at `frontend/` (stale; Phase 4 rewrites them).
- **Blueprint:** Gustify (`/home/khujta/projects/apps/gustify`) — `shared/design-tokens.ts` → prefixed Tailwind namespace → Storybook 10 + addon-vitest → `AppSurface` platform frames (mobile 390 / tablet / desktop + `chromeless` live escape hatch) → DESIGN.md grammar → per-feature screen batches (`Mobile*/Tablet*/Desktop*` × state exports) → smoke scripts → flows last.
- **Visual references (reference, not DOM contract):**
  - `docs/mockups/` — gastify's frozen HTML suite: 41 screens, locked 3-theme × 2-mode design language (Normal "Ni No Kuni" / Professional / Mono), DTCG `assets/tokens/tokens.json` v2.
  - `/home/khujta/projects/bmad/boletapp/docs/mockups/` — legacy 2026-03 mockup project: ~25 themed screens, 13 flows, redesigned 5-tab IA (Inicio | Compras | Escanear | Gastos | Perfil) locked in its HANDOFF.md.
  - `/home/khujta/projects/bmad/boletapp/docs/rebuild/UX-PLAN.md` — 17 locked UX decisions.
  - `frontend/src/**/*.stories.tsx` — 90 frozen stories (state inventories).
- **Token strategy (REVISED by DM-1, 2026-06-10):** `shared/design-tokens.ts` is the single TS source, exposed as `gt-*` Tailwind utilities via a generated `tokens.css` (`@theme inline`). The semantic role names + `gt-*` utility vocabulary are KEPT (so components don't churn), but the VALUES are now the Gustify Playful Geometric **single theme** (violet/amber/pink/emerald on cream/slate-900) + geometric grammar tokens (hard zero-blur offset shadows referencing ink). The earlier warm 3-theme port of `web/src/styles/global.css` is superseded. Fonts: Outfit (body) + Baloo 2 (display/wordmark) retained for now — typography (Outfit-extrabold-everywhere vs Baloo display) is a Phase-4/5 polish item.

## Phases

| # | Phase | Description | Tier | Complexity | Exec | Review | Commit | Push |
|---|-------|-------------|------|------------|------|--------|--------|------|
| 1 | Workspace + tokens foundation | Scaffold `design-lab/` (Vite + React 19 + TS + Tailwind 4 + Storybook 10 + addon-vitest); author `shared/design-tokens.ts` (6-variant port of web global.css + locked fonts/radius/shadows/motion); generated `tokens.css` + `gt-*` mapping; theme/mode toolbar; token showcase stories; 4 gates green (typecheck / build / build-storybook / test-storybook). | mvp | med | ✅ | ⬜ | ⬜ | ⬜ |
| 2 | Design-system core | `AppSurface` platform frames (390 phone w/ notch, tablet, desktop, `chromeless`), `StateTabs`, starter atoms (Button, Chip, Badge, Input, Card), nav organisms — BOTH IA candidates as presentational variants. | mvp | med | ✅ | ⬜ | ⬜ | ⬜ |
| 3 | IA + Style decision spikes | Comparison spikes: IA (11-entry vs 5-tab) + visual style (Boleta Cálida vs Geometría Gustify). **Style DECIDED → Geometría / Full Gustify palette (see Decisions §). IA still OPEN — locked in Phase 7.** | mvp | low | ✅ | ⬜ | ⬜ | ⬜ |
| 4 | Adopt Geometría (token + shell foundation) | Promote Gustify Playful Geometric palette into `shared/design-tokens.ts` as the SINGLE theme + geometric grammar (hard zero-blur offset shadows, ink borders); regenerate `tokens.css`; **platform toolbar (mobile/tablet/desktop) in Storybook**; `AppSurface` restyled geometric (3 platforms); single-palette + geometric-grammar token showcase; archive losing spike (Boleta Cálida); `DESIGN.md` (Gustify-shaped grammar). | mvp | med | 🔄 | ⬜ | ⬜ | ⬜ |
| 5 | Atoms (geometric) | Rebuild token-backed in the grammar: Button, Chip, Badge, Input, IconButton, PixelIcon — ink borders, hard shadows, extrabold, bounce-press, with stories. ✅ built + gated (2026-06-10). **Per-atom variation spikes** (`Design System/Spikes/*` from `design-system/_spikes/`): Button (Pop/Chunky/Press/Pill), Chip (Solid Violet/Amber/Outline/Underline), Badge (Pill+shadow/Tag/Flat/Soft), Input (Outline/Chunky/Filled/Underline), IconButton (Square-md/Square-lg/Circle/Soft). **INTERACTIVE (Gustify view pattern via `AtomSpike` harness):** Controls-panel `option` select (A/B/C/D + Compare) + `platform` radio (mobile/tablet/desktop) — selected option renders inside the AppSurface device frame; "Compare" = static grid. AWAITING per-atom picks; on choice, fold into the production atom + archive the spike. DESIGN.md atom rules pending (rolls up into Phase 4 doc). | mvp | med | ✅ | ⬜ | ⬜ | ⬜ |
| 6 | Molecules (geometric) | **category-token config (DM-4)** + `CategoryChip`/`CategoryLabel` (config-driven color+icon for L1–L4) + standard `Label` (date/location); Card family, StateTabs, CompactRow/CompactRowList, MetricCard/SummaryStats, EmptyState, Toast — geometric, token-backed, stories. Uses locked atoms (DM-3). | mvp | med-high | ✅ | ⬜ | ⬜ | ⬜ |
| 7 | Organisms (shell) — 4-tab nav + scan FAB (DM-5) | AppHeader, **BottomNav (4 tabs: Inicio·Compras·Gastos·Perfil)**, SideNav (4 + desktop title-FAB), **`ScanFab`** (desktop next-to-title / mobile-tablet bottom-right), NavDrawer, FullSurfaceSheet, FilterFlow — platform-aware, geometric. IA = DM-5 (resolved). | mvp | med-high | ⬜ | ⬜ | ⬜ | ⬜ |
| 8 | Inicio screen (full) | Rebuild Inicio from the new components — mobile/tablet/desktop × states (default/empty/loading). First fully-finished screen in the chosen system. | mvp | med | ⬜ | ⬜ | ⬜ | ⬜ |
| 9 | Screen batches | Compras (tx + items), Escanear (scan-mode/single/quicksave/manual/batch/statements), Gastos (trends/reports/insights), Perfil/settings, Groups — each feature's components/model/screens with platform×state stories. | mvp | high | ⬜ | ⬜ | ⬜ | ⬜ |
| 10 | Smoke harness + flows + handoff | `check:token-classes` + required-story baseline + Playwright nav screenshots; flow stories (scan→save, dashboard→drill→list); A4 handoff + consolidation map; Android token-adapter notes for `mobile/`. | mvp | med | ⬜ | ⬜ | ⬜ | ⬜ |

<!-- Exec is written by /gabe-mockup or /gabe-execute: ⬜ not started, 🔄 in progress, ✅ complete -->

## Decisions (lane-owned — promote to DECISIONS.md at commit time)

- **DM-1 · Style direction = Geometría / Full Gustify palette (2026-06-10).** After three side-by-side directions (Phase-2 baseline, Boleta Cálida, Geometría Gustify), the user chose **Full Gustify palette**: gastify adopts Gustify's Playful Geometric look — violet primary, amber/pink/emerald accents, cream canvas, slate-900 ink — as a **single theme** (drops the 3 warm themes × light/dark), rendered with Gustify's geometric grammar (2–3px ink borders, hard zero-blur offset shadows, bold Outfit, framed device surfaces, generous spacing). **Why:** "most inviting and interactive." **Consequences:** token layer is REPLACED (not extended) — `shared/design-tokens.ts` becomes the Playful Geometric single theme; the warm color vocabulary from `web/src/styles/global.css` no longer drives the lab (wiring into `web/` later means porting these tokens INTO web, not matching its current ones — a follow-up to flag when screens land). Boleta Cálida spike archived. Pixel-icon convention (DM-implicit) unchanged.
- **DM-2 · IA — RESOLVED by DM-5 (2026-06-10).** Navigation = Gustify 4-tab layout + scan FAB (see DM-5). The 11-entry `currentNavCatalog` is dropped; `redesignedNavCatalog` minus Escanear = the 4 tabs.
- **DM-3 · Atom treatments LOCKED (2026-06-10, per user spike picks).** Button = **A · Pop** (unchanged baseline: 2px border, 2px hard shadow, rounded-xl, hover-lift). Badge = **C · Flat pill** (pill + 2px ink border, NO shadow). Chip = **B · Amber** (active fills amber + ink text) — this is the GENERIC chip (date/location/standard labels); taxonomy category chips are config-driven (DM-4). Input = **D · Underline** (bottom 2px border only; focus→primary, error→danger). IconButton = **C · Circle** (round tile; `size` md/lg/fab — `lg` = content action options with bigger icon, `fab` = scan FAB). Spikes archived to `design-system/_spikes/archive/*.archive.tsx`; `AtomSpike`/`spikeLayout` harness kept for molecule spikes.
- **DM-4 · Category labels are config-driven (2026-06-10).** Taxonomy categories **L1 Rubro (12) / L2 Giro (44) / L3 Familia (9) / L4 Categoría (42)** each get a **distinct color + PixelLab pixel icon**, defined in a CONFIG FILE (e.g. `features/.../model/categoryTokens.ts`) and re-tinted for the Playful Geometric theme (port + retint the legacy `docs/mockups/assets/tokens/{storeColors,itemColors,groupColors,categoryColors}.ts`). A `CategoryChip`/`CategoryLabel` molecule reads {color, icon} from config by category id. NON-category labels (date, location, generic) follow a STANDARD background + icon (the generic `Chip` atom / a standard `Label` molecule). Pixel icons already exist (`rubro-*`/`store-*`/`item-*`/`familia-*`). **Open sub-decision:** color-derivation approach (port-and-retint legacy vs author fresh) — settle at Phase-6 start.
- **DM-6 · Spike-first iteration is the DEFAULT mode (2026-06-11, user-locked).** Every component tier — **assets → atoms → molecules → organisms → screens** — is explored via variation spikes BEFORE settling: an interactive spike (the `_spikes/AtomSpike.tsx` harness, exported as `Spike`) with an `option` picker (A/B/C/D + Compare) + `platform` picker (mobile/tablet/desktop) shows several iterations and their contrast; the user picks; the winner folds into the production component and the spike is archived. Each tier is built ON the decided tier below it (assets decided → atoms on assets → molecules on atoms → …). For molecules+ the variations are about LAYOUT / DISTRIBUTION / INFORMATION DENSITY (not just border/shadow). Spikes live in `design-system/_spikes/*` titled `Design System/Spikes/<Component>`; default the molecule+ spikes to `option: "compare"`. Applies going forward to all new components.
- **DM-5 · Navigation = Gustify 4-tab + scan FAB (2026-06-10).** Bottom nav has **only 4 destinations** (Inicio · Compras · Gastos · Perfil); the **Escanear (scan) action becomes a floating FAB inside the content section**, mirroring Gustify's "add ingredient" button — **desktop: next to the screen title; mobile/tablet: bottom-right corner**. Built in Phase 7 (AppShell BottomNav 4-tab + SideNav + a `ScanFab` organism); replaces the 5-tab BottomNav in the spikes/HomeScreen.
- **DM-7 · Legacy-faithful Transaction Detail + decomposition (2026-06-14).** Recreated the BoletApp `EditView` detail at legacy spacing/density (4px scale maps 1:1) re-skinned in geometric grammar; required data per item = name · total · unit price · units · category. Then **decomposed both Transaction Detail and Analytics Toolbar into per-tier parts** (DM-6 spike-first, one spike per part): new ATOMS `SegmentedToggle` (shared 2..n selector behind LevelToggle/CountModeToggle), `MetaPill`, `StepperButton`, `AttachTile`; new MOLECULES `MerchantHeader`, `ItemRow`, `ItemGroup`, `TransactionTotal`, `PeriodNav`, `LevelToggle`, `CountModeToggle`, `TreemapCell`/`MiniTreemap`; shared fixtures `transactionFixtures.ts` + `analyticsFixtures.ts`.
- **DM-8 · Payment indicator + picker (2026-06-14).** New relational payment concept (legacy had none): **no card data stored** — a method is a label + a `fin-*` pixel icon + an accent color; always one cash ("Efectivo", fixed coin) + 0–10 cards (`MAX_CARDS=10`). `PaymentChip` molecule; `paymentMethods.ts` config. Picker = real **Modal** atom + `PaymentPicker` (Efectivo + cards + "Agregar tarjeta") + `AddCardForm` (alias + fin-* icon + color). Payment shows in BOTH header and footer; tapping either opens the picker.
- **DM-9 · Transaction Detail part picks (2026-06-14, user spike picks).** Item Row = **D Category accent bar** (colored left edge = category color, category name + unit×qty in that color). Item Group = **A Tinted band** (familia-tint header; body re-laid as gap'd accent-bar rows, not divided). Merchant Header = **B Compact** (ThumbnailBadge + inline category/payment + one-line metadata) — CategoryChip + PaymentChip equal height (PaymentChip sm padding lowered to `py-0.5` to match CategoryChip sm). Total Footer = **C Amount on CTA** (total folded into the green save button, label left / amount right; "Pagado con" payment row above).
- **DM-10 · Analytics part picks (2026-06-14, user spike picks).** Period Nav = **A Plain steppers**. Level Toggle = **A Icons-only pill, refined**: bigger icons (26px) + on selection a **label-overlay** (`gt-label-reveal` keyframe in index.css) slides over the whole pill covering the options for ~5s, fading in/out, then revealing the options again (anti-spam through L1–L4). Count Toggle = **A Two-segment, refined as a SWITCH**: bigger icons + tight outer track (`p-0.5`, `gap-0.5`) hugging the two segments.
- **DM-11 · Diagrams/visualizations deferred (2026-06-14).** **Treemap Cell pick is DEFERRED.** It belongs to a future dedicated "diagrams & visualizations" spike pass: keep almost the same layouts but vary **colors + font families** across ALL charts/visualizations together, with more representative examples — tackled AFTER the transaction work. The `TreemapCellSpike` stays an ACTIVE spike until then; `TreemapCell`/`MiniTreemap` molecules exist as the working baseline.
- **DM-12 · Icon bump + payment chip soft-tint (2026-06-14, user correction).** Pixel icons were too small everywhere → proportional **~+25–30% bump** across the system (CategoryChip sm 14→18/md 18→24, PaymentChip 18/22, ThumbnailBadge badge 14→18/16→20, AttachTile 28→34/24→30, ItemRow 20→26, ItemGroup 18→24, MerchantHeader edit 12→16/cal 11→14, CountModeToggle 20→26 in h-10, AddCardForm tiles 22→28, PaymentPicker rows 22→26, IconButton story 20/22→26 + new Sizes story md26/lg30/fab34). **PaymentChip redesign:** the card's assigned color is now a **soft background tint** on the whole chip (`softBgFor` = saturated hex → ~15% rgba via `hexToRgba`; cash = white `--surface`), with ONLY icon + name on top — the **color-swatch circle is removed** (chip, picker rows, and add-card preview). Card-icon choices expanded 6→12 finance glyphs. Color picker still stores the saturated hue (softened at render). 92 tests green; visual proof captured.
- **DM-13 · Diagrams/visualizations pass kickoff (2026-06-14).** Thorough legacy analysis done (workflow `witjwezww`) → full piece inventory in `docs/rebuild/ux/DIAGRAMS-INVENTORY.md`: 6 families (F0 shared primitives, F1 treemap, F2 donut, F3 sankey, F4 trend-list, F5 chart-chrome, F6 story-reports). Per DM-11 the layouts stay ~legacy; the pass varies COLORS + FONT FAMILIES consistently. **Decisions:** (a) **Sankey = add ECharts to design-lab + a gastify-themed config** (echarts 6.1 + echarts-for-react installed). (b) ECharts installed; shared atoms `useCountUp`(pending)/CircularProgress/Sparkline/TrendChange built.
- **DM-13b · Diagrams approach CORRECTED (2026-06-14, user feedback).** The combined Chart-Skin board (one spike, all charts) was REJECTED — too small to judge any single chart, B/C indistinguishable, D too divergent. **New approach:** **(1) Token-True palette is the chosen base** (gt-chart-1..6 rotation) — held FIXED. **(2) ONE faithful spike PER DIAGRAM** (`Design System/Spikes/Diagram · <Name>`), each rendering the REAL legacy structure, not abstract mini-cells. **(3) A/B/C/D on each diagram vary DENSITY / LAYOUT TREATMENT** (gutter, ink border, category count, cell emphasis), NOT colors. **(4) Start from the exact legacy structure and iterate.** ChartSkinSpike archived. **FIRST: Treemap** — ported the legacy squarified algorithm verbatim (`lib/treemapLayout.ts`, Bruls/Huizing/van Wijk + step-height 320/400/480/560/640), faithful `TreemapCell` (3 densities tiny/compact/standard + Más folder + categoryCount badge + % ring + count pill + count-up amount), real `Treemap` organism (squarified placement + absolute cells), `Diagram · Treemap` spike (A Airy full/thin / B Geometric full/ink-border / C Focused fewer/roomy / D Focused-geometric). `TREEMAP_FULL` fixture (8 cats w/ value+count+itemCount+pct). 98 tests green; real squarified treemap proven via screenshots. Old TreemapCellSpike archived (superseded). **NEXT diagrams after treemap pick:** donut → sankey → trend-list → chrome → reports, same per-diagram-spike pattern.
- **DM-13c · Treemap decided + soft palette (2026-06-14, user pick).** Treemap = **A Airy** (thin 2px gutter, no cell border) folded as the `Treemap` organism default. **Palette softened:** the vivid full-saturation gt-chart hues were too bright → `tokenTrueSoftColor` renders each Token-True hue at a **~40% pastel tint** (still distinct per category, calm, dark ink text + ring crisp on top) and is now the **default treemap palette** (`tokenTrueColor` kept for the vivid variant). `TreemapSpike` archived. 98 tests green; soft vs vivid proven via screenshots. Soft-tint approach is the candidate skin for the remaining diagrams too (confirm per diagram).
- **DM-13d · Diagram tint LOCKED at 50% (2026-06-14, user pick).** Added a live `tint` range control + `TintLadder` story (20/30/40/55/70/100%) to the Treemap; user swept and chose **0.5**. Locked as `DIAGRAM_TINT = 0.5` in `organisms/Treemap.tsx` — the SINGLE source of truth for Token-True palette softness, used by `tokenTrueSoftColor`/`tokenTrueTint` and the default `tint` prop. Donut/sankey/trend/reports will all default to `DIAGRAM_TINT` so the whole analytics surface shares one softness; change the one constant to re-tune everywhere. 99 tests green.
- **DM-17 · Item row split into TWO layouts (2026-06-14, user rethink).** Scanned-transaction items rarely map to an icon, and the txn vs history contexts want different data → two distinct row layouts, each spiked (decision-first, production ItemRow untouched). **Layout 1 — Transaction item row** (`Spikes/Item Row · Transaction`): NO icon (no reliable mapping); shows title · category · unit price · units · total; user lean = two-line total-prominent (A); A/B/C/D vary arrangement. **Layout 2 — Items-History row** (`Spikes/Item Row · History`): HAS a mapped ingredient/meal icon (future Gustify connection) → framed IconTile; over-period aggregate; A/B = total-spent framing, C/D = last-price framing (user wants both spiked). Added `HistoryItem` + `sampleHistoryItems` fixture. 108 tests green; both spikes proven. **REFINED per user (DM-17b):** Layout 1 = **merged A+B** — no icon, title + content-width category chip UNDER it (left), total over "$unit ×qty" stacked (right). Layout 2 = same base **+ left mapped IconTile**; right = total spent on top, then **txn count ("N compras") + toggle arrow**; toggling EXPANDS the receipts containing that item as **CompactRows** (store ThumbnailBadge + date·×qty·$unit + line total), **capped at 5**, then a **"Ver todo →"** button (links to txn-history). Added `HistoryPurchase` + `purchases[]` to the fixture. 108 tests green; expanded state proven. **DM-17c (decided):** History count = **single pill** holding receipt icon + N + chevron (one bordered pill); expanded receipts span the **FULL item width** (no inset), only the "Ver todo" button stays inset. History spike collapsed to the one decided design. 108 tests green; collapsed-pill + full-width-expanded proven. **DM-17d (decided):** History row drops the left icon → **same base as the txn row**; instead, a **purple Gustify-link chip** sits inline AFTER the name (icon-only, `bg-gt-primary`), shown **ONLY for items mapped to Gustify** (ingredients/prepared-foods with a Gustify match). Uses REAL Gustify ingredient icons copied into `public/gustify-icons/` (bread/milk/chicken-breast/coffee from gustify `generated-icons/ingredients`); `PixelIcon` gained a `dir` prop ("pixel-icons" | "gustify-icons"). `HistoryItem.gustifyIcon?` optional (Café molido left unmapped to prove the conditional). 108 tests green; mapped+unmapped proven. **NEXT: fold both layouts into production ItemRow variants** (txn merged-A+B · history = no-icon + Gustify chip + expandable receipts) + wire txn variant into ItemGroup.
- **DM-34 · Reports REBUILT — 4 timeframes + 2 sections + density escalation (2026-06-15, user).** The user clarified the real legacy Reports model: reports come in FOUR timeframes (weekly/monthly/quarterly/annual) with ESCALATING density (annual = most complete), each showing TWO sections — establishments (where you buy) + items (what you buy). My DM-32 spike (one generic donut + dashboard) was too thin. Studied the legacy `reportYearGeneration.ts`/`ReportDetailOverlay` (workflow `w1hfwiuzw`) → `docs/rebuild/ux/REPORTS-TIMEFRAMES-SPEC.md`. **Key realization:** density escalation is NOT 4 layouts — it's the SAME report populated with progressively more: **hero + 💡 insight + 🏆 highlights (monthly/quarterly/annual ONLY) + 🏪 establishments section (donut + group cards) + 🛒 items section (donut + group cards)**. Weekly = floor (top-3 groups, no highlights); monthly = +highlights +uncaps to ALL groups; quarterly = +persona hook; annual = richest (all months, 2-cat insight). Both sections always generated; gated by data presence not timeframe. **Built:** `lib/reportTimeframeFixtures.ts` (`TIMEFRAME_REPORTS` for W/M/Q/A — establishments [6 store rubros] + itemFamilias [5 item familias], each group = header data + line-item rows; highlights/hook per period; weekly slices top-3; `sectionSegments` → donut). Molecule `ReportGroupCard` — the legacy CategoryGroupCard/ItemGroupCard: a category-tinted tone-card, header `[icon][name][%-chip] … [spark][amount][±%]` over translucent line-item rows `[icon][name·N] … [spark][amount][±%]` (items `hideRowIcons`); tiny direction-colored sparklines + TrendChange. Molecule `ReportDetail` — the timeframe-aware full report (hero MetricCard + insight/hook StatusCard + highlights StatusCard[M/Q/A] + 🏪 Section[donut + group cards] + 🛒 Section[donut + group cards, no row icons]); a Section renders its `SpendingDonut` only when >1 group. **Rebuilt the Reports spike** `Diagram · Reports`: A Semanal(floor) / B Mensual(+highlights, all groups) / C Trimestral(+hook) / D Anual(richest) / E interactive Semanal/Mensual/Trimestral/Anual `SegmentedToggle` driving one report so the density visibly escalates. Full gate green (typecheck/build/build-storybook), **126 tests green**; weekly floor (top-3, no highlights) vs annual richest (all groups + highlights + hook) + both sections + group cards + line-item rows + section donuts proven via screenshots. **SETTLED (user): reports are STATIC SNAPSHOTS — generated once, stored as objects, never re-generated even if underlying transactions change (point-in-time artifacts). The 4-report layout is good — CONSOLIDATED.** Spike archived → `_spikes/archive/ReportSpike.archive.tsx`; active Spikes section empty. Production `ReportDetail`/`ReportGroupCard`/`SpendingDonut` + `TIMEFRAME_REPORTS` fixture are the settled Reports surface (the static-snapshot nature is a data/storage concern, not a component change — the molecules already render a fixed report object). **ALL ANALYTICS DIAGRAMS + REPORTS COMPLETE.** **Mockup status: assets ✅ · atoms ✅ · molecules ✅ (incl. all 5 diagrams + reports) · organisms ⬜ (nav/header NOT yet legacy-studied — only early generic AppShell scaffolding) · screens ⬜.** NEXT per user: the navigation + header bars (Phase 7 organisms) — taken from legacy, spike-first, PixelLab for any new icons (same as Gustify), BEFORE rebuilding the screens. NOTE: prior DM-32 generic `ReportCard`/`ReportCarousel` (Instagram story cards) remain as separate molecules — they were the legacy `ReportCard`/`ReportCarousel` (NOT wired into the detail overlay); kept available, but the timeframe report detail (this DM) is the real Reports surface.
- **DM-33 · CORRECTION — Reports spike NOT yet reviewed; un-archived (2026-06-15, user).** I had archived the Reports (and earlier Trend) spikes prematurely — the user picked from a *screenshot* of the compare grid, not from an interactive spike session like Sankey. Per the user ("we didn't do the spike on reports… do the spike on reports first"), **un-archived `ReportSpike.stories.tsx`** back to active (`_spikes/`, import `../AtomSpike`→`./AtomSpike`) so the A/B/C/D Reports spike can be explored LIVE in Storybook and settled properly. The earlier DM-33 "SETTLED on B" claim is **RETRACTED** — Reports is NOT settled; the molecules are built (SpendingDonut/ReportCard/ReportCarousel/ProgressDots, DM-32) but the presentation pick stands open pending the real spike review. (The prior pick of "B" from the screenshot is a lean, not a settled decision.) Spike order of record: …→ Sankey (settled) → **Reports spike = current/active**. Trend was also fast-archived; revisit if the user wants its spike re-opened too.
- **DM-32 · Reports diagram built — the 5th/LAST analytics piece (2026-06-15).** Per-diagram pattern (after Trend): faithful legacy Reports + A/B/C/D spike. Legacy `boletapp/src/features/reports/` studied (workflow `wushf5000`) → `docs/rebuild/ux/REPORTS-SPEC.md`. **Scope:** a flat snapshot family (static donut + story cards + light carousel); screen hub + PDF export + timed auto-advance DEFERRED. **Built** molecule `SpendingDonut` — the STATIC report donut: pure-SVG filled `describeArc` WEDGES (ported verbatim: `RING_THICKNESS=0.42`, `SEGMENT_GAP=2°`, `MIN_SEGMENT_ANGLE=8°`, `polarToCartesian` -90 start, percent-desc sort) re-skinned to Token-True 50% fills + white separation strokes + a faint `gt-line`@0.2 bg ring; NON-interactive (no hover/click/anim/hit-area, `<title>` tooltip only); `DonutCenterLabel` center total + an inline read-only side legend (icon disc · name · %). Did NOT reuse `DonutRing` (stroked circles + required selection + always-on reveal — wrong for a snapshot). Molecule `ReportCard` — full-screen Instagram-story card: centered icon (emoji or pixel) + title + big display `primaryValue` + `TrendChange` pill + secondary/description; **gradient-by-type** bg (summary=violet, trend=blue [card-local hex], milestone=amber→orange [card-local hex], category=flat surface — card chrome, not the locked palette); `isActive` opacity/scale. Molecule `ReportCarousel` + atom `ProgressDots` (active dot widens to a violet pill; ←/→ keyboard; no timer). **Additive component extensions:** `TrendChange.onGradient` (translucent `bg-white/20` white pill for dark cards) + `MetricCard.deltaSlot?` (a TrendChange instead of the Badge delta). Net-new `lib/reportFixtures.ts` (`PeriodReport` + `SAMPLE_REPORT` + 4 typed `ReportCardDatum`s; reuses SEGMENTS/TOTAL_SPEND/TRENDS_RICH/clpK, net-new only prevTotal/change/txnCount/itemCount/insight/cards). Stories `SpendingDonut` (Default/NoLegend), `ReportCard` (Types/Carousel). Spike `Diagram · Reports`: **A** donut+legend snapshot / **B** story cards (4 gradients) / **C** carousel+dots / **D** summary-stat dashboard (MetricCard hero + boletas/ítems/promedio + compact donut + top-3 TrendRows + insight StatusCard). Full gate green (typecheck/build/build-storybook), **126 tests green**; static donut ($385k center + 6-row legend) + 4 gradient story cards + carousel/dots + dashboard composite + all 4 spike panels proven via screenshots. **DRILL DECISION (corrected from DM-29):** Reports is a FLAT snapshot — it does NOT drill (nav is list→detail→close + a lateral jump to filtered History; the legacy `DrillDownGrid` is a different 2-axis machine, out of scope). So the DM-29 "extract `useDrillDown<T>` at Reports (rule of three)" trigger is **FALSE** — the hook still has exactly TWO real consumers (donut + trend). **HELD the extraction:** keep the two mirrors; do NOT refactor the shipped/green donut+trend for ~0 coverage. Extract the day a real 3rd single-axis `{root, childrenOf}` consumer lands (a future drillable treemap/category sub-list), then migrate all three with parity proof. **AWAITING reports pick (A/B/C/D).** **All 5 analytics diagrams now built: treemap · donut · sankey · trend · reports.**
- **DM-31 · Trend SETTLED + equal-width fix + spike archived (2026-06-15, user).** Picked **A layout + direction spark color** (both already the defaults — no change). **Equal-width fix:** rows had different widths because the legacy "emerge-from-behind" chevron was a separate bordered tab present ONLY on drillable rows (poked out past the leaf rows). Rebuilt the row as **ONE bordered card with a constant-width chevron slot at the right edge** (icon when drillable, equal-width empty `span` on leaves) — every row now identical width (verified: single distinct width 284 + right edge at root AND drilled levels). **Name-room tweak (user):** putting the chevron inside ate ~20px of the name column → shrank the Sparkline 56→**44px** + tightened the spark↔amount gap `gap-gt-6`→`gap-gt-2` (2px) + amount stack minWidth 52→48, freeing the name column (Restaurantes/Vivienda/Más now show full; the eye still lands on the amount). Spike retired+archived → `_spikes/archive/TrendSpike.archive.tsx` (import `./AtomSpike`→`../AtomSpike`); active Spikes section empty. Full gate green (typecheck/build/build-storybook), **122 tests green**; equal widths (root + drilled) + freed name room proven via screenshots + DOM width measurement. **Trend diagram COMPLETE.** **NEXT + LAST diagram: Reports** (static SpendingDonut + report/story cards) — where the shared `useDrillDown<T>` hook gets extracted across donut+trend+reports per the DM-29 deferred decision.
- **DM-30 · Trend row = FAITHFUL legacy distribution (2026-06-15, user — "none of the options cut it; the sparkline uses too much space; legacy had a better distribution").** The DM-28 A/B/C/D spike layouts were my own inventions; read the REAL legacy `TrendListItem.tsx` and reproduced its distribution 1:1. **The legacy row** (its own comment): `[Icon] Name + count-pill (stacked) │ [tight: ~~56px spark~~ · amount/change stacked] │ ›`. The balance my first pass lost: the **name+count column is `flex-1`** (eats the row → full names, no truncation; count pill stacked BELOW the name), and the sparkline is a **SMALL FIXED 56px (`w-14`) butted right against the amount/change stack (6px gap)** as one tight right cluster — so the spark never dominates, the eye lands on the amount. `TrendRow` rewritten to exactly this: IconTile · `flex-1` name(+categoryCount badge)/count-pill stack · shrink-0 cluster(`<Sparkline width=56 height=24>` + `gap-gt-6` + right-aligned `clpK` amount over `TrendChange`) · drill chevron as a separate `-ml-gt-8` zone emerging from behind the card (legacy's `marginRight:-8px` overlap). Direction-color (DM-28 §2) + grey "Más" + drill (DM-29) all preserved. **Dropped the invented `layout`/`TrendRowLayout` knobs** (sparkPlacement/amount/changePill/dense) — the legacy distribution IS the answer, not a density axis; `showCount` is the one remaining row prop. **Spike retired to the single real choice:** `Diagram · Trend` now A direction-color vs B category-color (the only open decision); the 4 density options are gone. Stories: Default (faithful + drill + Más) / NoCount / CategoryColor. Full gate green (typecheck/build/build-storybook), **122 tests green**; faithful row (full names, count pill under name, tight 56px-spark+amount/change cluster, spark no longer dominating) + drill into giros (breadcrumb/back, names full at depth) proven via screenshots. **AWAITING only the spark-color pick (direction vs category — direction recommended/default).**
- **DM-29 · Trend drill-down (2026-06-15, user directive — "every diagram drills").** The trend list gains the drill the donut has (legacy `TrendsView` had it). Legacy drill studied (workflow `w4egf107c`); built faithfully + consistent with `DonutChart`. **Data:** added additive `TREND_DRILL_TREE: Record<string, TrendRichDatum[]>` + `trendDrillChildren(id)` to analyticsFixtures — mirrors the donut's `DRILL_TREE` (root ""=TRENDS_RICH; SAME branches/ids: supermercados→[Supermarket,Wholesale]→[food-fresh,food-packaged,hogar]→[BreadPastry,DairyEggs,MeatSeafood]; transporte-vehiculo→[GasStation,AutoShop]) but each child carries the trend payload (sparkline/change/dir/count). All child ids resolve in getCategoryToken (verified). **Per-level re-percentage baked in** (children sum to ~100% of parent — legacy re-scoped percent to the new parent). Leaves (L4 + the non-food giros) have no tree entry → chevron hidden. TRENDS/TRENDS_RICH untouched. **`TrendList`:** added drill state mirroring `DonutChart:51-75` (a `path` stack, `current = trendDrillChildren(deepest) ?? data`, `drill`/`back` bumping internal `animKey` to replay the stagger; `externalAnimKey` prop seeds+syncs for parent-driven replay; `defaultCanDrill = id!=="otros" && trendDrillChildren(id)!=null`; controlled mode when `onDrill` supplied). Added the donut-style **breadcrumb + back header** (back button = CSS rotated-square glyph, title = deepest segment, breadcrumb = "Total › Supermercados › …" chain; renders only when `path.length`). **`TrendRow` unchanged** (chevron + name-drill + isMas guard already correct). Spike/stories fed `TRENDS_RICH` root + a drill note; A/B/C/D layout knobs untouched (drill orthogonal to layout). Full gate green (typecheck/build/build-storybook), **122 tests green**; full L1→L2→L3→L4 drill (chevrons only on drillable rows, leaves have none) + breadcrumb growth ("Total › Supermercados › Supermercado") + back climb-out + direction-colored child rows + stagger replay all proven via screenshots. **DEFERRED DECISION (recorded):** keep per-component drill mirrors (donut + trend) for now — do NOT extract a shared `useDrillDown<T>` yet (would force a refactor of the shipped donut, regression risk; the shared nucleus is ~15 lines; treemap/sankey have structurally different geometry/peel state and won't use it). **Extract `useDrillDown<T>(root, children, labelFor) → {path,current,drill,back,canDrill,animKey}` at REPORTS** (the 3rd id→children-stack consumer = rule of three), migrating donut+trend+reports together with spike parity proof. **AWAITING trend layout pick (A/B/C/D) — drill now applies at every level.**
- **DM-28 · Trend diagram built (2026-06-15).** Per-diagram pattern (after Sankey): faithful legacy trend list + A/B/C/D density spike. Legacy `TrendListItem`/`TrendsView` extracted (workflow `w1vg0kss6`) → `docs/rebuild/ux/TREND-SPEC.md`. **Reuse confirmed AS-IS:** `Sparkline` atom (64×24 SVG path + <2pt dashed fallback, `color`/`strokeWidth`), `TrendChange` atom (▲▼=✦, up=red/down=green spending-semantics), `IconTile`, `getCategoryToken`. **Built** molecule `TrendRow` — IconTile · name+`clpK` amount stack · optional count pill (DonutLegend's) · `Sparkline` · `TrendChange` · optional drill chevron; structural cousin of DonutLegend's row. **Sparkline color = `colorMode`** (TREND-SPEC §2): default **"direction"** (the legacy semantic split — up→`--negative-primary` red, down→`--positive-primary` green, neutral→`--text-tertiary` grey, color-coordinated with TrendChange; the ONE documented place the trend steps outside the locked distribution palette — a good/bad *semantic* axis, not a re-coloring) with **"category"** (Token-True 50% skin) as a compare. Grey **"Más"** aggregate via `id:"otros"` (grey token natively) + a `categoryCount` outline badge + grey-forced spark + never-drills, sinks to the bottom. Molecule `TrendList` — value-desc sort (Más last), owns the legacy **entrance stagger** (`visibleRows` Set, `i*60ms` translateX(-20)→0 slide, `prefers-reduced-motion`→all-at-once, `animKey` replay). Flat top-level (no drill peel — donut shows the level model; `canDrill`/`onDrill` pass through). Added additive `TRENDS_RICH` (6 rows + `otros` Más + `categoryCount` + count/itemCount) — `TRENDS` untouched. `layout` knobs (sparkPlacement right/background/under-name/mini, sparkWidth/Height, amount inline/headline/omit, changePill, showCount, dense). Stories `TrendList` (Default/WithMas/CategoryColor); spike `Diagram · Trend`: **A** sparkline-right compact (legacy baseline) / **B** sparkline-as-backdrop (faint full-bleed) / **C** two-line stat-prominent (headline amount + change pill, spark under name) / **D** dense legend-like (count pill + mini spark + Más row) / **E** direction-vs-category color compare. Full gate green (typecheck/build/build-storybook), **122 tests green**; direction-colored rows (Super ▼-12% green, Transporte ▲+8% red) + Más grey row + categoryCount badge + count pills + all 5 spike panels proven via screenshots. **Observed:** A/D truncate names hard at 320px (count pill+spark+% squeeze the name col — "Más"→"M.."); **B/C show full names** (backdrop/two-line free the horizontal space). **AWAITING trend pick (A/B/C/D + the §2 direction-vs-category color decision).** **NEXT diagram: Reports** (the last — static SpendingDonut + report cards).
- **DM-27 · Sankey PROPAGATED + spike archived; Trend diagram started (2026-06-15, user directive).** Sankey is settled (DM-22→26), so propagated + archived per the per-diagram pattern. **Propagate:** `SankeyChart.stories.tsx` rewritten so the canonical **`Default`** story IS the settled design (`<SankeyChart iconNodes levelSelector/>` = floating-disc icon-nodes + LevelToggle-style L1–L4 peel + 3s tap pulse + default 460px); the decision-process stories (`IconNodes`/`IconNodePlacement`/`LevelSelector`) were folded out, replaced with `TextLabels` (bare ECharts reference) + `EmbeddedInBar` (the alt). Production `SankeyChart` defaults already encode the pick (`iconPlacement="above"`, height→460 in selector mode) — no component change needed; screen embeds use it as-is. **Archive:** `SankeySpike.stories.tsx` → `_spikes/archive/SankeySpike.archive.tsx` (import path `./AtomSpike`→`../AtomSpike` fixed; `.archive.tsx` is outside the `**/*.stories.@(ts|tsx)` glob so it's no longer a live story). Active Spikes section now empty. Full gate green; **118 tests** (was 120: −1 archived 4-option spike, net −2 from folding intermediate stories into 3 canonical ones). **NEXT diagram: Trend** (the 4th per DIAGRAMS-INVENTORY: trend LIST — TrendListItem row = IconTile + name + count pill + Sparkline + count-up amount + TrendChange + drill chevron, "Más" grey aggregate). Legacy study workflow `w1vg0kss6` in flight (legacy trend list + sparkline geometry + direction-color semantics → build spec + A/B/C/D spike plan); existing atoms `Sparkline`?/`TrendChange`/`IconTile` + `TRENDS` fixture to reuse.
- **DM-26 · Sankey floating-disc pick + LevelToggle-style peel (2026-06-15, user directive).** Two changes from the spike review. **(1) Picked the floating-disc treatment** (`iconPlacement="above"` is now the icon-node DEFAULT, embedded-in-bar demoted to the alternate). Reworked the floating branch so it does the DM-25 3s PULSE: a tinted disc floats ~26px above each thin bar showing the icon; on tap the disc's icon swaps to the node's NAME in place (the disc grows into a name pill), then reverts after 3s with the band amount/% + adjacency dim. Bumped the icon-mode top inset to 42px (from 24) for the `above` case so the top-row floating discs clear the box (embedded keeps 24). **(2) Re-skinned `LevelRangeBar` to the `LevelToggle` look** — same visuals (icons-only round h-8/w-8 segments in an ink-bordered `overflow-hidden` rounded-pill track, violet fill, oversized to bleed into the corners and clipped clean), but the fill is the **range PEEL** (covers the contiguous 2–4 levels) instead of LevelToggle's single-level circle; the press state machine (`pressLevel`, the extend/retract/new-low rules + 2-level floor) is unchanged. Dropped the old icon+code+name chip layout + the `dense` prop. Spike (`Diagram · Sankey`) updated: floating-disc is now A/B/C across 340/460/560px canvas heights (the "hard to see" fix — taller canvas opens up the 4-level view), embedded-in-bar is the D alternate. Full gate green (typecheck/build/build-storybook), **120 tests green**; floating-disc rest + LevelToggle-style peel at L2–L3 and expanded L1–L4 + node tap→disc-becomes-name pulse all proven via screenshots. Composes with DM-23/24/25. **SETTLED on spike pick B — floating disc · 460px:** `height` is now optional and DEFAULTS to **460** in `levelSelector` mode (340 otherwise), an explicit `height` always wins; the props `height?` de-duped. The spike repointed to A=settled (floating @ default 460, switch platform to judge) with 340/560 kept as references for the deferred per-platform (mobile/tablet/desktop) height tuning + D the embedded alt. Verified the LevelSelector story now measures 460px. Per-platform display/height is explicitly DEFERRED to the mobile/tablet/desktop pass.
- **DM-25 · Sankey icons embedded-in-bar + 3s tap pulse + spike (2026-06-15, user directive).** Three changes to the icon-node sankey: **(1) icons EMBEDDED in the bars** (were floating discs on thin bars) — in `on-bar` mode the node band is fattened to **44px** + given the 2px ink border, and the PixelIcon sits flush-centred IN the band (the band frames the glyph). **Fixed a positioning bug this exposed:** the overlay used `getBBox()` which returns LOCAL (pre-transform) coords, so the series-group's top/left inset translate was dropped and every icon shifted up-left by the inset (icons perched on the bar's top edge). Switched to `getBoundingClientRect()` made container-relative (true on-screen position, DPR-independent) — icons now centre exactly in their bands. `extractNodeRects` now takes the container el. **(2) Tap = a 3s timed PULSE** (like LevelToggle's reveal, DM-14): on tap the icon DISAPPEARS and the node's NAME renders in its place (centred in the band) + band shows amount/% + non-adjacent nodes dim; after **REVEAL_MS=3000** everything reverts (name→icon, band clears, dim clears) — user-confirmed "everything reverts after 3s". A `selTimer` ref holds the pending revert (cleared on re-tap/unmount); tapping the same node again clears immediately; text-label mode keeps the old sticky toggle. **(3) Moved the diagram into the Sankey spike** (`Spikes/Diagram · Sankey`, rebuilt from the superseded DM-22 density spike) — the settled interaction (embedded icons + 3s pulse + level peel) shown at A 340px / B 460px / C 560px canvas heights + D floating-disc runner-up, across mobile/tablet/desktop via the platform radio, to fix the "hard to see" cramping when the peel covers 4 levels (taller canvas = the fix). Full gate green (typecheck/build/build-storybook), **120 tests green**; embedded-centred icons (close-up) + 3s name-swap-then-revert + spike compare grid all proven via screenshots. Composes with DM-23 (icon-nodes) + DM-24 (level peel).
- **DM-24 · Sankey level-range peel selector (2026-06-15, user directive).** A top bar of four chips **L1·L2·L3·L4** (Rubros·Giros·Familias·Categorías) with a sliding **peel** that always covers a CONTIGUOUS range of ≥2 adjacent levels; the sankey renders exactly that range of the taxonomy. **State machine `pressLevel(range,k)`** (pure fn in analyticsFixtures, range = `{lo,hi}`): `k<lo`→extend down (`lo=k`); `k>hi`→extend up (`hi=k`); `k===lo`→retract the low end (`lo+1`) **but only if the range stays ≥2 wide** (else no-op); `lo<k≤hi`→k becomes the new low end (clamped so `k===hi`→`lo=hi-1`). Confirmed against every user example: L2–L3 →press L1→ L1–L3 →press L4→ L1–L4 →press L1→ L2–L4 →press L2→ L2–L4 (then →press L2→ L3–L4); and the **2-level floor** (user-confirmed): at L3–L4, pressing L3 or L4 is a **no-op** — from the minimum you can only ADD levels. Changing the range clears any node/link selection. **Data:** added an additive 4-level taxonomy to analyticsFixtures (`SANKEY_TAXONOMY_NODES`/`_LINKS`, each node tagged `level:1|2|3|4`, links across every adjacent pair, all ids resolve in getCategoryToken; L2 carry brand labels Líder/Central/Nido) + `sankeyForLevels(lo,hi)` slices it to a range (nodes in-range + links between consecutive kept levels) + `SANKEY_LEVEL_META`. The original 3-level `SANKEY_NODES`/`SANKEY_LINKS` are untouched (immutability; still the non-selector default). **Components:** new molecule `LevelRangeBar` (4-col grid of icon+code+name chips + an absolutely-positioned violet-soft ink-bordered peel spanning `lo..hi`, `transition-all` slide; in-peel chips read violet, the rest muted). `SankeyChart` gained `levelSelector` (+`defaultRange`, default L2–L3): holds the range, derives `activeNodes`/`activeLinks` from the slice (props ignored in this mode), wires `pressLevel`, and the ECharts `levels` depth array is now dynamic (range width) not hardcoded 3. All downstream uses (idToLabel/total/option/adjacency/extraction) switched to active*. Story `LevelSelector` (level bar + icon-nodes). Full gate green (typecheck/build/build-storybook), **120 tests green**; the full press sequence + expand-to-4-levels + retract + 2-level-floor no-op + diagram re-slice all proven via screenshots. Composes with DM-23 icon-nodes (the user's working diagram).
- **DM-23 · Sankey icon-nodes + tap-to-reveal name (2026-06-15, user directive).** "Instead of labels, use icons, and when pressed the name appears below the icon." Activated the dormant icon-in-node path (`iconNodes` prop, default false → text-label mode untouched, zero regression). **Mechanism:** ECharts text labels hidden (`label.show:false`); a React **PixelIcon disc overlay** floats over each node, positioned from the laid-out node rects. **Position extraction — the load-bearing risk, resolved differently than the research recipe:** the recipe's `getModel().getSeriesByIndex(0).getGraph()` path is DEAD in the production (minified) Storybook bundle — `getModel()` returns an object whose accessors are name-mangled (`getSeriesByIndex`/`getGraph` absent), and `convertToPixel` has no sankey coord system. **Pivoted to reading the rendered SVG** (renderer:"svg"): node `<path>`s are identified by their ink `stroke` (#1E293B) + matched to fixture nodes by `fill` rgb → `GT_CHART_HEX` hue index (node `i` = hue `i%6`), then `getBBox()` gives CSS-px rects 1:1 with an `absolute inset-0` overlay (the SVG fills its plot box). Wired on the `'finished'` event (fires post-700ms-animation, positions final) + a `requestAnimationFrame` immediate read (retries while the SVG is unpainted) + `ResizeObserver→inst.resize()`. **Disc** = 32px tinted circle + 2px ink border + 20px PixelIcon, centred ON its bar (`on-bar`, default — anchors unambiguously; the `above`-the-bar variant collides with the hint text + floats in the ribbon, proven worse in the `IconNodePlacement` 2-up). Top/bottom chart insets bumped (24/30px) in icon mode so edge-row discs + the name plate clear the box. **Tap** routes through the existing node-select handler (`selectNodeById`, shared with ECharts edge clicks): selected disc gets a purple ring + its **name plate reveals below the icon** (absolute, never nudges the disc); the band above shows **amount/% ONLY** in icon mode (no duplicate name pill — identity is now on-node); non-adjacent discs dim to 0.3 in lockstep with ECharts adjacency downplay (adjacency set built from `links`). **Fixed a fixture bug surfaced by the discs:** the L2 giro node ids were the `store-*` ICON names (`store-supermarket`/`store-restaurant`) which don't resolve in `getCategoryToken` → both middle nodes fell back to a generic "Otros" icon/name. Corrected `SANKEY_NODES`/`SANKEY_LINKS` to the real PascalCase giro ids (`Supermarket`/`Restaurant`); the brand display name ("Líder"/"Nido") stays in the node `label`, and the overlay shows the fixture `label` (brand) with the token `icon` (giro storefront). Stories `IconNodes` + `IconNodePlacement`. Full gate green (typecheck/build/build-storybook), **119 tests green**; icons-only default + node tap (ring + name-below + $182k/78% band + adjacency dim) + bottom-row reveal + giro-icon fix all proven via screenshots. NOTE: SVG fill-matching assumes the locked Token-True palette (the only icon-node config); a custom `colorFor` would break the mapping (acceptable — palette is FIXED per DM-11).
- **DM-22 · Sankey diagram built (2026-06-15).** Per-diagram pattern (after Donut): faithful legacy vertical sankey + locked skin, A/B/C/D density/layout spike. Legacy ECharts config extracted (workflow `w4j0vkime`) → `docs/rebuild/ux/SANKEY-SPEC.md`. **Built** molecule `SankeyChart` — ECharts 6 vertical sankey (`orient:"vertical"`, rubro→giro→familia: Supermercados/Restaurantes → Líder/Nido → Frescos/Envasados), svg renderer, node fills FIXED to Token-True 50% (`tokenTrueSoftColor` from `diagramSkin`; "otros"→grey), ribbons `lineStyle.color:"source"` + curveness/opacity, Outfit-700-11 ink labels, `tooltip:{show:false}`, `emphasis.focus:"adjacency"`. A node/link tap → tinted **SelectionTitle** pill (`getCategoryToken` tint/color + PixelIcon) with amount+% (link = `source › target`), and `dispatchAction` downplay→highlight dims the unconnected branch. Density knobs are props: `nodeWidth`/`nodeGap`/`curveness`/`linkOpacity`/`labelPos`("inside"|"right"|"bottom")/`inkBorder`. Spike `Diagram · Sankey`: **A** Airy ribbons (14/10, 45% opacity, labels inside — legacy) / **B** Bold bars + ink (24px, 2px ink border, labels right) / **C** Dense flow (8px, curveness 0.6, 55% opacity, labels bottom) / **D** Faint river (18px, gap 16, 30% opacity). Full gate green (typecheck/build/build-storybook), **117 tests green**; render + node-selection (Líder → "Líder $182k (78%)" pill + adjacency dim) + all 4 treatments proven via screenshots. **Observed in proof:** B's `labelPos:"right"` CLIPS the right-column node labels off the card at mobile width (Restaurantes→"R", Nido→"N", Envasados→"E") — defect as written; A and D read nearly identical (only 45%↔30% opacity differs); C is the most distinct. **AWAITING sankey pick (A/B/C/D).** **NEXT diagrams: trend → reports.**
- **DM-21 · Donut drill-down + full legend (2026-06-15, user — re-studied legacy).** The DM-20 donut had simplified the legend + dropped drill-down. Restored both per the legacy `useDonutDrillDown` + `DonutLegend`. **DonutLegend** rebuilt to the full legacy row: IconTile · name · amount + magnitude bar · **count pill** (transactions/items per countMode, tappable) · percent · **drill chevron** (only when `canDrill`). **DonutChart** molecule (new) = ring + DonutCenterLabel + header (back button + breadcrumb "Total › Supermercados › …" + CountModeToggle) + full legend, owning the drill state: tapping a legend chevron drills into that section's next taxonomy level (L1 rubro → L2 giro → L3 familia → L4 categoría → subcat), re-animating; back climbs out. Drill data = `DRILL_TREE` / `drillChildren(id)` in analyticsFixtures (real taxonomy ids — Supermarket/Wholesale/food-fresh/BreadPastry…; "otros" + leaf nodes don't drill). `SegmentDatum` gained count/itemCount. Spike rebuilt to the full DonutChart (A Airy / B Geometric ink-wedges / C Focused side-legend / D Dense). 116 tests green; **L1→L2→L3 drill + breadcrumb/back + count toggle + full legend rows proven via screenshots.** **DECIDED: A Airy** (legacy look — ring 14/16 flat, full legend below). Production `DonutChart` already defaults to Airy (ring/inkBorder/side unset); spike archived. **NEXT diagram: Sankey** (extraction in flight).
- **DM-20 · Donut diagram built (2026-06-15).** Per-diagram pattern (after Treemap): faithful legacy donut + locked skin, A/B/C/D density/layout spike. Legacy extracted (workflow `wkjj5osje`) → `docs/rebuild/ux/DONUT-SPEC.md` (verbatim geometry). **Factored the shared diagram skin out of Treemap → `lib/diagramSkin.ts`** (GT_CHART_HEX, hexA, tokenTrueColor, DIAGRAM_TINT=0.5, tokenTrueTint, tokenTrueSoftColor, DiagramColorFor type); Treemap re-exports for compat. **Built:** atom `DonutCenterLabel` (absolute 2-line center overlay, reactive total↔selected). atom `DonutRing` — multi-segment stroke-dasharray ring ported verbatim (viewBox 120, rotate(-90), r=52, 0.5% gap carved at slice end, negative-offset accumulator, 14/16 stroke, selection thicken+dim-others-to-0.4, clockwise 80ms stagger reveal); palette FIXED via `colorFor`=tokenTrueSoftColor + `inkBorder` knob (two-circle ink-outline); a wider transparent hit-area circle makes the whole wedge band clickable (fill=none strokes alone were fragile). molecule `DonutLegend` — rows (IconTile chip + name + 70px relative magnitude bar + percent + optional drill chevron), `compact` mode (dot+name+%); selection LIFTED + synced with ring via shared {selected,onSelect} (`bg-gt-primary-soft`). spike `Diagram · Donut` A Airy(legacy 14/16 flat, legend below) / B Geometric(16/18 ink-border wedges) / C Focused(ring left + compact side legend, top-5+otros) / D Dense(thin 12/14, selected-% center, tight legend). 115 tests green; ring render + segment selection (center swaps to $182k/Supermercados/47%, others dim) + all 4 treatments proven via screenshots. **AWAITING donut pick (A/B/C/D).** (Static report donut `SpendingDonut` deferred to the reports pass.) **NEXT diagrams: sankey → trend → reports.**
- **DM-19 · Spacing-scale propagation (2026-06-15).** Migrated the older components to the `gt-N` spacing scale (DM-16) for full cohesion — **200 substitutions across 28 design-system files** (atoms Button/Badge/Chip/Input/Modal/SegmentedToggle/MetaPill/AttachTile/TrendChange; molecules Card/CompactRowList/Toast/Label/StateTabs/SummaryStats/MerchantHeader/TransactionTotal/PeriodNav/LevelToggle/PaymentPicker/AddCardForm/TreemapCell/CategoryChip/PaymentChip; organism AppShell; spike harness + TokenShowcase). Conversion was a SAFE 1:1 px-equivalent swap (Tailwind step×4px → `gt-<px>`, e.g. gap-2→gap-gt-8, p-4→p-gt-16, px-3.5→px-gt-14, py-2.5→py-gt-10) via a boundary-anchored regex touching ONLY spacing prefixes (gap/p/px/py/pt/pb/pl/pr/m*); sizing (w/h), positioning (inset/top/-bottom), borders, radius untouched. SummaryStats arbitrary-variant `[&>*]:pl-4`→`pl-gt-16` fixed by hand. Visually transparent — typecheck/build/build-storybook green, **108 tests green**, MerchantHeader/SummaryStats/TransactionTotal parity proven via screenshots. (Story files left on raw Tailwind — demo code, low priority.)
- **DM-18 · Item rows CONSOLIDATED to production (2026-06-15).** Folded both decided layouts into production molecules; both item-row spikes archived (`_spikes/archive/`), Spikes section now empty. **`ItemRow`** rewritten to the TRANSACTION layout (DM-17 merged A+B): no icon, title + content-width CategoryChip on the left, total over "$unit ×qty" stacked right; `tone="negative"` reddens the total; `index` prepends a small number (Original view); `accessory` overrides the right slot. Wired into `ItemGroup` unchanged (its `<ItemRow item density/>` calls still valid). **`HistoryItemRow`** (new molecule) = the Items-section row: no left icon + inline purple **Gustify-link chip** after the name (only when `item.gustifyIcon`), total spent + count-pill (receipt icon + N + chevron) on the right; tapping expands the receipts (`CompactRow`s, full-width, capped `cap`=5) + "Ver todo →". Stories for both. 108 tests green; ItemGroup + HistoryItemRow (collapsed + expanded) proven. **Item-row work COMPLETE.**
- **DM-17e · Chip icons enlarged app-wide (2026-06-14, user).** Icons inside pills/chips were too small → bumped so they sit closer to the pill border, with vertical padding REDUCED to compensate so PILL HEIGHTS are unchanged (+ `overflow-hidden` to clip). CategoryChip (sm 18→22/md 24→28, py-0.5→0 / py-1→0.5), PaymentChip (sm 18→22/md 22→26, same padding trick), History spike Gustify chip (14→20) + count-pill receipt (13→18) + chevron (h-3.5→h-4). CategoryChip sm + PaymentChip sm stay equal height (both icon 22, py-0). 108 tests green; bigger-icons-same-height proven.**
- **DM-16 · Gustify layout polish BUILT (2026-06-14, all proposals approved).** Applied the whole approved set; conventions in `docs/rebuild/ux/LAYOUT-CONVENTIONS.md`. **Foundation:** (a) **spacing scale** — `spacing` map in design-tokens.ts → `--spacing-gt-N` → `gap-gt-10`/`p-gt-16`/etc (off-grid 6/10 first-class); (b) **typeface = Outfit everywhere** (Baloo 2 dropped; `font-gt-display` aliased to Outfit); (c) **radius softened** (Gustify parity: lg 8→10, xl 10→12, 2xl 12→16, 3xl 14→20…). **Atoms:** `IconTile` (framed icon well, sm/md=44/lg/hero=64, optional tint); Chip selection-depth (unselected soft border/no shadow, active ink border + shadow; SegmentedToggle already conformed). **Molecules:** **ItemRow** rebuilt to 3-col `[44px·minmax(0,1fr)·auto]` grid = IconTile (category-tinted) + name(extrabold) + **category CHIP filled with category bg color** + medium-muted unit×qty meta + **trailing amount PILL**; `tone="negative"` flips the pill red. **ItemGroup** = tone-card (category color border+bg) + header order **icon·name·round-count-badge … amount·CSS-corner-chevron** (gear dropped) + 1px hairline rows + aria-controls panel. **StatusCard** (new) = tinted surface + solid bordered icon disc + extrabold title + medium body, 4 tones. **EmptyState** = framed hero IconTile + stacked→sm:row actions. 106 tests green; all proven via screenshots. Typeface/radius now LOCKED (were deferred in DM-15).
- **DM-15 · Gustify emulation study + approved layout direction (2026-06-14).** Thorough Gustify study (workflow `wafj70awf`) → `docs/rebuild/ux/GUSTIFY-EMULATION.md` + an HTML review doc `docs/rebuild/ux/gustify-emulation-review.html` (current-vs-proposed mockups, reasons, tradeoffs; user requested docs-not-code). **APPROVED to build:** (1) **Spacing scale** — add `spacing` map to design-tokens.ts → `gt-N` utilities (the missing cohesion primitive; keep off-grid 6/10). (2) **ItemRow → Despensa style** — framed 44px IconTile + name + meta + trailing amount/qty PILL, 3-col `[44px·minmax(0,1fr)·auto]` grid, meta drops to font-medium muted; **TWEAK: the item category shows as a CHIP/pill filled with that category's background color** (not only via the tile). (2b) **Row state colors** — `tone` prop; trailing pill flips red for negative/urgent. (3) **ItemGroup → tone-tinted card** (category color border+bg) + 1px hairline rows + round 28px count badge + CSS-corner chevron; **TWEAK: header order = icon · group name · count … amount · toggle arrow, and DROP the gear/action button** (no button between amount and chevron). **Toggle white-corner fix DONE** (oversized selection circle bleeds into rounded corners, clipped by overflow-hidden — circles kept, not squares). **Sections 4 (smaller adopts: IconTile atom, medium-muted meta, StatusCard, EmptyState, selection-depth) + 5 (typeface, radius scale) PENDING user decision — building visual examples.** Typeface (Baloo vs Outfit) + radius scale deferred / per-component.
- **DM-14 · Molecule polish pass (2026-06-14, user review).** Five fixes: **(1) Toast** — always `rounded-gt-2xl` (rounded RECTANGLE, straight sides) instead of pill→2xl, and the expanded detail has NO divider line — the whole toast stays one shape as rows are added below. **(2) ItemGroup** — header is now JUST a title (show/hide); body dropped its `p-2`/gap inset so items span the FULL width of the title. **(3) ItemRow** gained a `flush` prop = full-width flat row (no rounded tile/gap/hover-lift, keeps the left category accent edge) used inside ItemGroup; default stays the standalone rounded accent-bar tile. **(4) Shared narrow analytics-control height** — PeriodNav (StepperButton already h-8), LevelToggle, CountModeToggle all unified to **h-8** segments, and the toggles lost their track padding/gap (`p-0`/`overflow-hidden`) so the segment circles butt directly against the track + each other (true switch look, no white gap). **(5) LevelToggle reveal 5s→3s** (REVEAL_MS + `gt-label-reveal` keyframe). 99 tests green; all five proven via screenshots.

## Current Phase

Phase 6.5: Molecule variation spikes (DM-6 default mode) — 🔄 AWAITING PICKS (2026-06-11). Layout/distribution/density spikes added under `Design System/Spikes/*` via the `Spike` harness (option A/B/C/D + Compare, platform mobile/tablet/desktop): **Transaction Row** (Two-line / Dense single-line / Category accent-bar / Card-per-row), **Hero Metric** (Stacked / Inline-delta / With-mascot / Split-banner), **Toast** (Icon-left tint / Accent-bar / Compact single-line / Header-bar). Default option = `compare`. User picks per molecule → fold into the production molecule + archive spike. (Card/StateTabs/EmptyState/CategoryChip already settled; spike them on request.)

- **DM-39 · Roadmap B — browse screens BUILT (2026-06-15).** First `Features/*` screens on `Nav.tsx` chrome (NOT AppShell — P0 rule). **Legacy studied** (HistoryView + ItemsView via Explore agents): date-grouped receipt cards, meta pills (time/location/item-count), expandable item preview; items view aggregates by name+merchant with count badge + expandable receipt list. **Gustify studied** (FilterFlow/SearchRow/RecipeFilterPanel): SearchRow (icon+input+action grid), FilterFlow accordion facets (one open at a time, chip selection, floating apply/clear footer), FilterTriggerRow (icon stack of selected tokens). **Built:** (1) `lib/browseFixtures.ts` — `BrowseTransaction`, `DateGroup`, `BrowseFilters`, `FilterFacet`/`FilterFacetOption` types; `BROWSE_TRANSACTIONS` (4 date groups, 9 txns), `BROWSE_FACETS` (category/period/location/sort); `activeFilterCount`. (2) molecule `SearchRow` — gastify port of Gustify's search row: PixelIcon + search input + action slot, geometric grammar (ink border, hard shadow, focus ring). (3) molecule `TransactionCard` — the receipt row: ThumbnailBadge + merchant name + total + meta pills (time/location/item-count badge) + expandable preview items (first 3 + "+N más"). (4) organism `FilterSheet` — Gustify FilterFlow port: full overlay with header (eyebrow+title+active-count+close), accordion facets (one-at-a-time, Chip selection), floating apply/clear footer. `FilterTriggerButton` (filter icon with active-count badge). (5) `Features/Compras/ComprasScreen` — transactions browse: AppHeader(browse) + SearchRow + active-filter badges + date-grouped TransactionCards + BottomNav + ScanFab(corner) + FilterSheet overlay. (6) `Features/Gastos/ItemsBrowseScreen` — items browse: AppHeader(browse) + SearchRow + HistoryItemRow list (DM-17d Gustify chips + expandable receipts) + BottomNav + ScanFab + FilterSheet overlay. Stories: SearchRow (Default/WithFilterAction), TransactionCard (Default/Expanded/List), FilterSheet (Default/WithSelection/TriggerButton), ComprasScreen (Default in AppSurface), ItemsBrowseScreen (Default in AppSurface). Full gate green (typecheck/build/build-storybook), **142 tests green**. Both screens follow DM-37 Storybook structure (`Features/<area>/<screen>`). **NEXT: spike the browse layouts (DM-6 pattern — A/B/C/D density variations for TransactionCard + the overall browse layout).**
- **DM-40 · Reports spike SETTLED = Option E → `ReportViewer` + browse-row/filter refinements CONSOLIDATED (2026-06-16, user picks).** The DM-33 open item (Reports presentation never settled via a live spike) is now CLOSED. Re-activated the timeframe-density spike (`Diagram · Reports`, A/B/C/D = W/M/Q/A + E interactive selector) for live review; user picked **E (the interactive timeframe selector)**. Promoted E into a production molecule **`ReportViewer`** (wraps the pure `ReportDetail` from DM-34): header row = **"Resumen" title + a BARE Lucide share glyph top-right** (no container; `ShareIcon` added to `assets/icons.tsx`; fires `onShare(period, value)` — PDF export presentational for the mockup), a **full-width timeframe bar** (`SegmentedToggle fill`), and a **value step-selector below it** (`‹ value ›` center-value + side controls, same pattern as the transactions-filter Period navigator) backed by a new `REPORT_PERIOD_VALUES` map; changing the timeframe resets the value cursor to that timeframe's current period. Stories `ReportViewer` (Default/StartWeekly/StartAnnual). Spike archived → `_spikes/archive/ReportSpike.archive.tsx` (Spikes section now empty). **Also consolidated this session (DM-17/DM-39 follow-ups):** (1) **FilterSheet rebuilt** to the Gustify recipe-filters look — selection slots + 3s name-flash on add, no chevron (row is the toggle), amber open-facet highlight; **Category** = multi-select icon grid (max 3, category-tint bg, count, disable-at-limit); **Período** = linked year/quarter/month/week timeline (big-arrow steppers, cascade-down + roll-over-up, range W1/2025→current); **Sort** = dimension single-select + direction up/down toggle (enabled once a dim is picked, items "Por fecha" / compras "Por día"); selection model now array-valued (`{[facetId]: string[]}`) + `selectionCount`. (2) **`SortIcon`** added (replaced the pencil on "Ordenar por"). (3) **Item-row link flow** (`LinkItemFlow.tsx`): every `HistoryItemRow` has a full-height **left slot** — matched = real Gustify icon on the item's **category-tint** bg sized to Gustify's pantry tile (`h-11 w-11`, 28px icon); unmatched = dashed "+" placeholder → opens **`LinkSourcePopup`** (Buscar en Gustify / Buscar en Gastify) → **`AddItemSheet`** (Gustify: Ingrediente/Preparado; Gastify: placeholder Producto/Servicio type picker; search + Cancelar/Vincular). Full lane green (typecheck + build-storybook). **NOTE:** ReportViewer value-stepper re-renders the same `TIMEFRAME_REPORTS[period]` for every value (one populated report per timeframe in fixtures) — paging shows the nav UX, not distinct per-period data; wire real per-period data later. Gastify-sheet taxonomy (Producto/Servicio) + the edible/matchable classification are placeholders to scope later. **NEXT (user-directed): the profile menu** (top-right avatar on mobile / nav section on desktop) listing Notificaciones · Historial de productos · Historial de transacciones · Reportes · Ajustes · Cerrar sesión — `ReportViewer` is the surface behind "Reportes".
- **DM-38 · Consolidation pass before roadmap B (2026-06-15, via gabe-mockup review).** Ran the gabe-mockup consolidation review (workflow `wrg695coo`) — verdict: **baseline CLEAN** (one locked diagram skin across all 5 charts, full `gt-*` scale adoption, correct atomic taxonomy, correspondence report PASS 54 stories/0 findings). Fixed all **P1 drift** in one pass: **(OTROS_GREY)** centralized the "Otros/Más" aggregate grey → one `OTROS_GREY="#9CA3AF"` in `diagramSkin.ts`, consumed by SankeyChart (`MAS_GREY`) + SpendingDonut (was a raw `#9CA3AF`). **(DIRECTION_COLOR)** the up=red/down=green raw-color map was defined 3× → exported ONE `DIRECTION_COLOR` from `atoms/TrendChange.tsx` (the canonical direction-semantics atom); `TrendRow` + `ReportGroupCard` now import it (deleted their local copies). **(token swaps)** `Nav.tsx` FAB `text-[28px]`→`text-gt-4xl`, `ReportCard` `text-[44px]`→`text-gt-7xl`, `TrendRow`/`ReportGroupCard` `mt-0.5`→`mt-gt-2` (all exact-match). **(donut back-chevron)** `DonutChart`'s back button aligned to the canonical `TrendList` one (added `ml-0.5` optical centering + `aria-hidden` + `duration-150 ease-gt-bounce`). **(icon outliers)** `chart-calendar` 12→14, `action-edit` 16→18. **(Reports canon — user pick)** the **timeframe report `ReportDetail` (DM-34) is CANON**; gave it a proper story (`ReportDetail.stories.tsx` — Weekly/Monthly/Annual), so `ReportDetail`/`ReportGroupCard` are no longer orphaned; the DM-33 story cards (`ReportCard`/`ReportCarousel`) are kept + documented as the SECONDARY "wrapped/share" surface. **(tone-card)** kept ItemGroup (comfortable) vs ReportGroupCard (compact) as two SANCTIONED tone-card densities rather than force-merge two intentionally-different contexts. **P2 accepted as-is** (DonutChart-vs-SpendingDonut intentional, deferred `useDrillDown<T>`, ECharts/gradient literal exceptions, device-frame arbitraries). Full gate green, **132 tests green**. **P0 (the one real consolidation): the old generic `organisms/AppShell.tsx` is superseded by `Nav.tsx` (zero live consumers) but still imported by the pre-rebuild `HomeScreen` → RULE going forward: all new screens build on `Nav.tsx`/`MAIN_NAV`, NEVER AppShell; AppShell + the losing IA catalog get deleted when Phase 8 rewires HomeScreen.** **NEXT: roadmap B on Nav.tsx — Compras + Items browse sections + Gustify-recipe-style filters (first `Features/*` screens).**

- **DM-37 · Nav SETTLED — Gustify icon+label bar + desktop left panel + Storybook Features structure (2026-06-15, user picks).** Settled all the open nav/header questions from the Gustify-style direction: **(A FAB) corner bottom-right** = kept (the square-plus from DM-36). **(BottomNav) Gustify layout = icon + section NAME below** — added the label under each 32px icon (`text-[10px]`, filled-active tile reads violet); no longer icon-only. **(D desktop) the Gustify LEFT PANEL** — rebuilt `SideNav` to Gustify's left rail: a `gastify` wordmark header with the **options/collapse icon top-right** (`ChevronLeft`, rotates when collapsed → `w-18` icon rail), **icon+label nav rows** (filled-active = ink border + primary-soft + shadow; soft-border inactive), and a **profile block at the bottom** (avatar + name + email). Removed the old `rail`/`footer` SideNav API + dup props. Spike OptionD now shows the one settled left panel (header+content+corner FAB inside). **(C Perfil) = both** (tab for browse + avatar/profile-block for the quick menu) — already supported. Full gate green, **126 tests green**; icon+label bottom nav + Gustify left panel (wordmark + top-right options + filled-active rows + profile block) + corner square-plus FAB proven via screenshots. **PROPAGATION NOTE:** the new `organisms/Nav.tsx` is the CANONICAL chrome for all upcoming screens; the old generic `organisms/AppShell.tsx` (still imported by the pre-rebuild `HomeScreen`/home spike) stays until Phase 8 rebuilds those screens on `Nav.tsx` — retire it then (avoids a risky cross-cutting refactor now). **STORYBOOK STRUCTURE (user — mirror Gustify):** Gustify = `Design System/{assets,atoms,molecules,organisms}` + **`Features/{Auth,Compras,Cooking,Despensa,Profile}/<screen>`**. gastify adopts the same: keep `Design System/*`, and put every SCREEN under **`Features/<area>/<screen>`** (areas like Inicio·Compras·Gastos·Perfil·Scan·Auth·Settings). All roadmap-B/C/D screens go under `Features/*`. Chrome (Nav.tsx) stays under `Design System/Organisms`. Canonical chrome showcase = `organisms/Nav.stories.tsx` (MobileShell · Headers · DesktopLeftPanel · Perfil); the NavHeader spike is archived (`_spikes/archive/NavHeaderSpike.archive.tsx`). 129 tests green. **NEXT: roadmap B — Compras + Items browse sections + Gustify-recipe-style filters (first `Features/*` screens).**
- **DM-36 · Nav/header re-skinned to GUSTIFY chrome style (2026-06-15, user).** "Follow more the Gustify style for spacing, icon sizing, and nav-bar style (NOT bg colors); back + profile too big + shouldn't be in a container; filter/lookup icons bigger; add-transaction FAB = the Gustify square-plus button." Gustify chrome extracted (workflow `w7x4hez45` — exact px from AppShell/IconImage/FloatingActionButton/SearchRow). **Re-skin rule: Gustify geometry, gastify colors.** Applied to `organisms/Nav.tsx`: **BottomNav** → Gustify nav bar (`border-t-[3px]`, `grid-cols-4 gap-gt-4`, padding `px-gt-6 pt-gt-6 pb-gt-2`, **tab icons 24→32px**, filled-active tab = `rounded-gt-lg border-2 border-gt-line-strong bg-gt-primary-soft shadow-gt-xs` vs transparent inactive). **Back** → BARE far-left arrow, NO container/circle (dropped the IconButton box; just `ChevronLeftIcon h-7 w-7` as the first flex child, `-ml-gt-4`). **ProfileAvatar** → smaller bare 32px (`h-8 w-8`, lighter — was h-9 + shadow). **HeaderAction** (filter/search) → kept Gustify's boxed 40×40 (`h-10 w-10 rounded-gt-md border-2`) but **glyph 20→32px** (Gustify md+, "bigger like the other lookups"). **ScanFab → square-plus** (Gustify QuickAddButton): 52px (`h-13 w-13`) `rounded-gt-lg` SQUARE (not pill), literal `+` glyph `text-[28px] font-extrabold`, `border-2 + shadow-gt-sm`, `bg-gt-primary`; **TAP opens the scan-mode sheet** (single/batch/statement — user pick: the "+" = "add a transaction", modes are how). Header gap `gt-12→gt-8`, actions wrapped `gap-gt-6` (Gustify rhythm). Dropped the now-unused IconButton import. Full gate green, **126 tests green**; square-plus FAB + filled-active Gustify nav + bare back arrow + bigger filter/search icons + smaller bare avatar all proven via screenshots. **Still AWAITING the spike picks (A FAB placement · C Perfil · D desktop).** OPEN follow-up flagged to user: whether filter/search should ALSO go bare (no box) like the back arrow, or keep Gustify's boxed treatment.
- **DM-35 · Nav + header chrome organisms built (2026-06-15) — roadmap A.** First screen-chrome piece. Legacy BoletApp shell studied (workflow `wkirnvkrx`) → `docs/rebuild/ux/NAV-HEADER-SPEC.md`. **IA reconciliation (the load-bearing decision): legacy + DM-5 AGREE on 4 tabs + scan-as-FAB** (legacy comment confirms "5th nav item is Alerts not Settings" → 4 tabs + center scan FAB, not 5). So tab COUNT is fixed; only FAB placement + Perfil treatment + desktop chrome are open. **Built** `organisms/Nav.tsx` (the legacy-studied replacement for the generic AppShell scaffolding): `MAIN_NAV` (4 tabs Inicio·Compras·Gastos·Perfil, pixel icons nav-home/nav-history/chart-pie/nav-profile); `BottomNav` (grid-cols-4, PixelIcon icon-only no labels, active=violet-soft halo, alerts dot, no scan-tab); `SideNav` (desktop, w-60 or icon-rail w-16); flexible `AppHeader` with a **variant enum** (home=wordmark+avatar / browse=title+search+filter / detail=back+title / settings=back+subtitle / period) + `band` slot + ProfileAvatar trailing in every variant; `ScanFab` (net-new — wraps IconButton size="fab", 3 placements corner/title/bar-center, right-click/long-press mode-selector popover single/batch/statement); `PerfilMenu` (legacy ProfileDropdown re-roled — Compras/Productos/Reportes/Metas[disabled]/Configuración); `HeaderAction`. **Icons:** all nav/header pixel icons already present; the ONE gap (Back) added as a stroke `ChevronLeftIcon`+`ArrowLeftIcon` in icons.tsx (utility action, zero PixelLab cost) — NO generation needed. **Spike** `Spikes/NavHeader` (first screen-chrome spike, in `AppSurface` phone frames): A scan-FAB placement (corner DM-5 vs bar-center legacy) · B header variants (home/browse/detail/settings) · C Perfil treatment (tab vs avatar-menu, C3 both recommended) · D desktop chrome (SideNav w-60 vs icon-rail). Full gate green (typecheck/build/build-storybook), **126 tests green**; corner-FAB anchors inside the phone shell above the 4-tab bar, header variants, Perfil menu, desktop SideNav all proven via screenshots. **AWAITING picks (A FAB placement · C Perfil · D desktop).** Old generic `AppShell.tsx` (currentNavCatalog 11-entry, escanear-tab special-case) to be retired after the picks settle. **NEXT roadmap items: B browse sections + Gustify filters → C settings + cards → D scan flows → E Gustify map button.**

## Remaining-mockup ROADMAP (2026-06-15, user — wrap up all mockups this session)

Mockup tiers: assets ✅ · atoms ✅ · molecules ✅ (incl. all 5 diagrams + reports) · **organisms ⬜ · screens ⬜**. Source apps: **legacy BoletApp** (structure + flows) + **Gustify** (settings + filters). Each piece = legacy/Gustify study → spike-first (DM-6) → settle → PixelLab for any new icons (Gustify workflow). Order:

- **A · Nav + header chrome** (legacy) — (1) the IA / nav STRUCTURE (tabs, scan placement, what each header shows per screen); (2) the nav + header BARS as real organisms (BottomNav/tabs, AppHeader top bar, SideNav, NavDrawer, ScanFab). Reconcile with DM-5 (4-tab Inicio·Compras·Gastos·Perfil + scan-FAB) against what legacy actually ships. Replaces the generic `AppShell.tsx` scaffolding (never legacy-studied).
- **B · Browse sections + filters** (legacy structure + Gustify filters) — (3) the **Transactions** browse section/screen; (4) the **Items** browse section/screen (the items the user has captured); (5) **filters** to navigate BOTH — emulate **Gustify's recipe filters** (filter by property / timelapse / etc.).
- **C · Settings + cards** (Gustify) — (6) the **Settings** screen, inheriting Gustify's well-formed settings structure; (7) **Cards setup** (NET-NEW — was never set up): manage the different payment cards the user can configure (ties to the DM-8 payment concept).
- **D · Scan flows** (legacy) — the complete screen flows for (8) **single scan**, (9) **breakout scan**, (10) **batch scan** (taken from the legacy app).
- **E · Gustify integration affordance** (design now, integrate later) — (11) in the **item view**, a button to **map a scanned item → a Gustify item** (ingredient / prepared meal). Future integration: scanned items map into the item view + this map button. Design the affordance now.

(Filters + settings come from **Gustify**, not legacy — Gustify's recipe filter + settings are the well-formed references to emulate.)

---

Phase 7: Organisms — 4-tab nav + scan FAB (DM-5) — ⬜ NEXT (spike-first per DM-6). Build AppHeader, BottomNav (4 tabs: Inicio·Compras·Gastos·Perfil), SideNav (+ desktop title-FAB), `ScanFab` (desktop next-to-title / mobile-tablet bottom-right), NavDrawer, FullSurfaceSheet, FilterFlow — platform-aware, geometric. Then Phase 8 rebuilds Inicio from the new atoms+molecules.

Phase 6: Molecules (geometric) — ✅ COMPLETE 2026-06-11. **Category system (DM-4):** `src/lib/categoryTokens.ts` = full 4-level taxonomy — L1 Rubro (12) → L2 Giro (44) · L3 Familia (9) → L4 Categoría (42) = **107 tokens**, each {label, icon, color, tint}. Ported from legacy {groupColors,storeColors,itemColors,categories}.ts + retinted; L1+L3 carry distinct base colors, L2+L4 inherit parent color (distinguished by icon). `childrenOf`/`categoriesByLevel`/`getCategoryToken` helpers. `CategoryChip` (config-driven, soft default / solid emphasis) + standard `Label` (date/location). All 107 icons verified present. **All geometric molecules DONE:** Card (geometric rebuild — 2px ink border + hard shadow), StateTabs (amber-active), CompactRow/CompactRowList (rows compose CategoryChip + Label + Badge), MetricCard/StatValue/SummaryStats, EmptyState (dashed), Toast (4 tones, pixel status icons). Integrated transaction-row story proves the full stack composing. Gates green (55 stories). Known minor: `scan-error` icon reads as a plus not an X (flagged for regen). **NEXT: Phase 7** = 4-tab nav + scan FAB (DM-5), then Phase 8 rebuild Inicio.

Phase 5: Atoms — ✅ COMPLETE 2026-06-11 (treatments LOCKED via user spike picks, DM-3; spikes archived). Icon family regenerated as one coherent pixel set (153 icons, direct PixelLab SDK).

Phase 5 (orig note): Atoms (geometric) — ✅ built 2026-06-10. Phase 4 foundation done (token layer = Playful Geometric single theme + hard-shadow grammar; geometric `AppSurface`; token showcase reworked; Boleta Cálida archived) EXCEPT `DESIGN.md` (Gustify-shaped grammar doc) still to write — Phase 4 stays 🔄 until then. **Platform toolbar now drives every HomeScreen story** (mobile stacked / tablet 2-col + bottom nav / desktop 2-col + sidebar — three distinct layouts); `platformFromGlobals` reads the global. Phase-5 atoms rebuilt geometric (ink border + hard offset shadow + extrabold + bounce-press): Button, Chip, Badge, Input, IconButton (new, square tile for pixel/utility icons), PixelIcon (from Phase 3). Gates green: 16 files / 39 stories. **NEXT: Phase 6 — geometric molecules** (Card family, StateTabs, CompactRowList, MetricCard/SummaryStats, EmptyState, Toast, FilterTrigger). Storybook dev port 6008.

### History (pre-decision, 2026-06-10)
- **Phase 2 + restructure (Gustify separation):** shell organisms in `design-system/organisms/AppShell.tsx`; `features/home/` split model/components/screens/spikes; taxonomy contract `docs/rebuild/ux/STORYBOOK-STRUCTURE.md` rewritten for design-lab; typescript-reviewer findings all fixed.
- **Phase 3 spikes:** IA (11-entry vs 5-tab) + Style (Boleta Cálida vs Geometría Gustify). Icon convention locked: meaningful glyphs = PixelLab; only close/back/submit/cancel use stroke icons. 200-icon set in `design-lab/public/pixel-icons/` + `PixelIcon` + catalog story (`nav-profile.png` = mascot alias, regen when PixelLab balance allows). → Style decided (DM-1).

## Parallel-plan protocol

- `.kdbp/PLAN.md` (P16) is owned by another session — this lane NEVER edits it.
- LEDGER entries from this lane are tagged `[MOCKUPS]`.
- Shared-file writes are kept minimal and append-only (DECISIONS.md, STRUCTURE.md); coordinate at commit time via /gabe-commit.
- Commit/branch strategy: design-lab work should ship on its own branch (e.g. `feat/design-lab-mockups`), not on P16's branch — decide at first /gabe-commit.

## Risks

| Risk | Severity | Mitigation |
|------|----------|------------|
| Token drift between design-lab and web/ | med | Same CSS var names verbatim; `shared/design-tokens.ts` is the single source; Phase 9 coverage gate |
| IA churn after screen batches start | med | Phase 3 settles IA BEFORE any screen batch (Phases 5–8) |
| Parallel-session file conflicts (.kdbp shared files) | low | Protocol above; new-file-only bias |
| Storybook/Vite version incompatibility | low | Pin to Gustify-proven stack (Storybook 10.3 + Vitest 4 + browser-playwright); Vite 7 fallback to 6 |
