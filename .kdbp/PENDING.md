# Deferred Items

| # | Date | Source | Finding | File | Scale | Priority | Impact | Times Deferred | Status |
|---|------|--------|---------|------|-------|----------|--------|----------------|--------|
| P1 | 2026-04-24 | gabe-review | Phase 1 built Phase 5-11 production desktop surfaces early (Insights, Reports, Items, Scan×3, Group Hub, Auth, Consent) — scope drift from stated P1 exit artifacts. May conflict with P2-P3 atomic component crystallization | `docs/mockups/screens/*-desktop.html` | mvp | medium | moderate | 1 | open |
| P2 | 2026-04-24 | gabe-review | Style prompts shipped = 6 (mono/normal/organic/playful-geometric/professional/sketch). PLAN called for 6 legacy + 3 new = 9. No DECISIONS entry logging the trim | `docs/mockups/styles/*.prompt` | mvp | medium | low | 1 | open |
| P3 | 2026-04-24 | gabe-review | No WCAG AA a11y audit artifact for Phase 1 — 14 desktop screens × 6 theme×mode runtime variants lack contrast / focus / ARIA validation. Phase 13 audit gate will catch but fix multiplies | `docs/mockups/design-system.html` | mvp | low | moderate | 1 | open |
| P4 | 2026-04-24 | gabe-review | `piggy-bank.png` lives at `docs/mockups/screens/piggy-bank.png` — asset in screens/ folder instead of `assets/icons/` or `assets/raster/`. Minor organization drift | `docs/mockups/screens/piggy-bank.png` | mvp | low | negligible | 1 | open |
| P5 | 2026-04-24 | gabe-mockup retrofit audit | `.kdbp/CHANGES.jsonl` is lane-era relic (last write 2026-04-23 lane_rollback). Consider `git mv .kdbp/CHANGES.jsonl .kdbp/archive/CHANGES.jsonl` to preserve history while decluttering active KDBP surface. Non-blocking cleanup. **RESOLVED 2026-06-01:** git mv .kdbp/CHANGES.jsonl -> .kdbp/archive/CHANGES.jsonl (history preserved, active surface decluttered). | `.kdbp/CHANGES.jsonl` | mvp | low | negligible | 0 | resolved | 2026-06-01 |
| P6 | 2026-04-27 | scan-picker verification | F1: error dialog ignores `error.message` — all error variants render identical generic body. Differentiated codes work in console only. Rebuild must surface distinct error UX per code. See KNOWLEDGE.md 2026-04-27 entry. | rebuild scope | mvp | high | high | 0 | resolved | 2026-05-13 |
| P7 | 2026-04-27 | scan-picker verification | F3: currency-mismatch "Use default" overwrites currency code without FX conversion → data corruption (e.g., $48.50 USD becomes 48 CLP). Rebuild must convert server-side or remove the choice. See KNOWLEDGE.md 2026-04-27 entry. | rebuild scope | mvp | high | critical | 0 | rebuild-only |
| P8 | 2026-04-27 | scan-picker verification | F4: unknown-merchant scans — UI component exists (ScanResult renders "First scan at this merchant" when `is_new_merchant=true`) and is component-tested, but backend SSE `scan_complete` does not emit `is_new_merchant`. Integration path incomplete: need backend contract change or post-scan API fetch. | rebuild scope | mvp | medium | moderate | 1 | resolved | 2026-05-20 |
| P9 | 2026-04-27 | scan-picker verification | F5: low-confidence scans — UI component exists (ScanResult renders confidence badge + "Low confidence scan" alert when `confidence_score < 0.6`) and is component-tested, but backend SSE `scan_complete` does not emit `confidence_score`. Integration path incomplete: need backend contract change or post-scan API fetch. | rebuild scope | mvp | medium | moderate | 1 | resolved | 2026-05-20 |
| P10 | 2026-04-27 | scan-picker verification | F6: i18n keys leak to UI as literal strings (`scanError` rendered as title). Rebuild needs CI translation-coverage gate for all scan/editor keys. See KNOWLEDGE.md 2026-04-27 entry. | rebuild scope | mvp | low | low | 0 | rebuild-only |
| P11 | 2026-04-27 | gabe-review L1 | Retroactively review Phases 3 (molecules), 4 (flows + hub), L0 (legacy foundation) — all shipped Exec ✅ without a /gabe-review run, so their Review columns remain ⬜. Causes /gabe-review no-arg deterministic resolution to land on Phase 3 instead of Current Phase. Resolve by running `/gabe-review` with explicit targets, or accept and tick retroactively after a light smoke audit. **RESOLVED 2026-06-01:** obsolete — the legacy L0/Phase3/Phase4 mockup-plan structure was superseded by the D62 polling-fallback plan; there is no longer a stale Phase-3 review-collision to resolve. | .kdbp/PLAN.md | mvp | medium | moderate | 0 | resolved | 2026-06-01 |
| P12 | 2026-04-28 | gabe-mockup D18 cascade self-audit | Five L2a molecule triples (state-tabs, card-transaction, card-stat, card-empty, card-celebration) ship with class names invented from memory rather than extracted from canonical molecules. Affected files: `docs/mockups-legacy/molecules/{state-tabs,card-transaction,card-stat,card-empty,card-celebration}-{mobile,tablet,desktop}.html` + landings + `scripts/gen_molecule_triples.py`. **CLOSED 2026-04-28 by mockup-to-React pivot** — the path forward stopped being "fix the broken HTML triples in the legacy mockup track" and became "use the React app + Storybook as the mockup surface" (DECISIONS D24/D25/D26/D27). The R1+R2 gates this row was waiting on are no longer needed because no new HTML mockup work is happening. The 5 broken triples remain in `docs/mockups-legacy/molecules/` as a frozen baseline (test harness still exercises them; they document the failure mode but aren't a rebuild target). | docs/mockups-legacy/molecules/ | mvp | high | high | 0 | closed |
| P13 | 2026-04-28 | gabe-commit | Type errors in `frontend/src/__firebase-mocks__/firestore.ts` at lines 260 + 299 — `Query<T>` not assignable to `Query<DocumentData>`. **CLOSED 2026-04-28 in Phase 8 prep**: made `QueryConstraint.apply` generic (`<T = DocumentData>(q: Query<T>) => void`); made `queryToSpec` generic over T; widened `startAfter`'s assignment via structural cast (mock-only — outer T snapshot type and inner T query type aren't constrained to match in this mock). `npm run build` (`tsc && vite build`) now passes cleanly. Verified by Phase 8 production-bundle check (`scripts/check-prod-bundle.sh` — 6/6 checks pass on the emitted dist/). | `frontend/src/__firebase-mocks__/firestore.ts` | mvp | high | low | 0 | closed |
| P14 | 2026-04-28 | gabe-commit | `README.md` not updated when `frontend/package.json` added Tailwind 4 + test deps (vitest, @testing-library/react, jest-dom, user-event, jsdom) during Ladle pivot Phase 1. Defer to Ladle pivot Phase 9 — README rewrite there will cover the new dev workflow + Ladle setup + new dep set in one coherent pass rather than fragmenting across commits. | `README.md` | mvp | low | low | 0 | open |
| P15 | 2026-04-28 | gabe-commit | `.kdbp/PLAN.md` Current Phase = "Phase L2: mockups-legacy Molecules" but Ladle pivot Phase 1 (Tailwind CDN → built Tailwind 4) just shipped from a different plan. The real work has pivoted to `~/.claude/plans/okay-here-s-something-that-ancient-graham.md` per the user's scrap-and-pivot direction. Reconcile in Ladle pivot Phase 9: archive L2a/L2b/L3–L5 phases; add Ladle-pivot phases (0–9) with state-of-completion. Auto-tick precondition fails during this transition because the commit doesn't match Current Phase — silent no-op is correct, but the mismatch should resolve at Phase 9. **RESOLVED 2026-06-01:** obsolete — PLAN Current Phase is no longer "Phase L2"; it is the active D62 progress-polling-fallback plan. The Ladle-pivot transition the row tracked is long past. | `.kdbp/PLAN.md` | mvp | medium | medium | 0 | resolved | 2026-06-01 |

<!-- P1-P4 from /gabe-review Phase 1 (2026-04-24) — see LEDGER for review trace -->
<!-- P5 from /gabe-mockup retrofit audit (2026-04-24) — deferred cleanup per user Q3 -->
<!-- P6-P10 from scan-picker hands-on verification (2026-04-27). Status="rebuild-only" means do NOT fix in legacy port; carry into rebuild requirements. Detail in KNOWLEDGE.md "Verified UX Findings" entry. -->
<!-- P11 from /gabe-review L1 (2026-04-27) — closes review-scope-collision finding by tracking the cleanup work, not by lying about phase state. -->
<!-- P12 from gabe-mockup D18 cascade self-audit (2026-04-28) — see DECISIONS.md D23 + ~/.claude/plans/why-did-you-do-twinkling-lecun.md. Rebuild gated on R1+R2 enforcement landing first. SUPERSEDED 2026-04-28 by Ladle pivot — closes at Phase 9 archive. -->
<!-- P13-P15 from /gabe-commit on Ladle pivot Phase 1 (2026-04-28) — see ~/.claude/plans/okay-here-s-something-that-ancient-graham.md. P13 = pre-existing tech debt; P14+P15 = bookkeeping deferred to Phase 9 of the pivot. -->
| P16 | 2026-05-06 | gabe-review P1 | `_create_tables` mutates `Base.metadata` server_defaults globally (strips gen_random_uuid). Known SQLite test pattern but invisible side-effect on process-wide metadata. Fix: deep-copy metadata or per-fixture metadata. | `backend/tests/conftest.py:32` | ent | medium | moderate | 1 | open |
| P17 | 2026-05-06 | gabe-review P1 | Metrics endpoint `GET /api/v1/metrics` exposes operational data (counters, latencies, uptime) without auth. Standard at MVP; add auth or move to separate port at Enterprise. | `backend/app/api/metrics.py:12` | ent | medium | moderate | 2 | resolved | 2026-05-07 |
| P18 | 2026-05-06 | gabe-review P1 | `BaseHTTPMiddleware` has known limitations for streaming responses and websocket connections. Rewrite to pure ASGI middleware when P5 receipt image uploads or streaming features land. | `backend/app/middleware.py:7` | scale | low | high | 1 | open |
| P19 | 2026-05-06 | gabe-review P1 | No test verifies structlog JSON output format or that stdlib loggers (uvicorn, sqlalchemy) are properly captured. Verify at Enterprise when log pipeline (OTel collector) is in place. | `backend/app/logging.py:11` | ent | low | moderate | 1 | open |
| P20 | 2026-05-06 | gabe-review P1 | `MetricsRegistry` uses `threading.Lock` — viable at MVP/ent volume (microsecond critical sections) but may contend at Scale with high-throughput endpoints. Consider lock-free atomics or asyncio.Lock with async methods. | `backend/app/observability.py:11` | scale | low | moderate | 1 | open |

| P21 | 2026-05-06 | gabe-review P2 | ORM uses generic `JSON` type for `display_labels` columns but Alembic migration uses PostgreSQL `JSONB`. Autogenerate always flags a diff. Fix options: (a) `with_variant(JSONB(), 'postgresql').with_variant(JSON(), 'sqlite')`, (b) configure `compare_type` in alembic env.py, (c) accept the noise. Not a runtime bug. | `backend/app/models/reference.py:18` | mvp | low | low | 0 | open |

<!-- P16-P20 from /gabe-review Phase 1 backend (2026-05-06) — deferred as option [1] MVP-only fix. P16-P17 Enterprise gate, P18+P20 Scale gate, P19 Enterprise gate. -->
<!-- P21 from /gabe-review Phase 2 backend (2026-05-06) — JSONB→JSON revert; cosmetic autogenerate noise, not runtime defect. -->
| P22 | 2026-05-15 | gabe-review P3-Ph1 | Test harness exists (vitest + 4 optimistic mutation tests) but auth boundary, protected route redirect, token propagation, sign-in error remain untested | `web/package.json:6` | ent | medium | moderate | 1 | resolved | 2026-05-13 |

| P23 | 2026-05-13 | gabe-commit | New `@router.get` decorator in `backend/app/api/reference.py` (store-categories endpoint) — no file in `docs/` updated | `backend/app/api/reference.py` | mvp | medium | moderate | 0 | open |
| P24 | 2026-05-20 | user decision D44 | Runtime scan result needs an explicit review-warning signal for accepted v2-dev.9 minor-review risks: math failure, item-count mismatch, receipt-discount mismatch, or meaningful item amount discrepancy. UI must preserve original receipt item order for image side-by-side comparison and offer category grouping as a secondary view over the same items. | `backend/app/services/scan_worker.py`, `mobile/src/screens/*`, `web/src/components/ScanResult.tsx` | ent | high | high | 0 | open |
| P25 | 2026-05-20 | user decision D46 | Post-launch deployment escape hatch: plan and prove Render as the managed fallback platform after production launch. Scope should include backend deploy, WebSocket/SSE behavior, SPA hosting choice, Postgres/scheduler fit, storage portability, backup/restore drill, and staging smoke evidence. Do not block the current Railway MVP launch path on this item. | `docs/runbooks/*`, future Render config | ent | low | high | 0 | open |
| P26 | 2026-05-20 | classifier | Backend SCA audit has a targeted `PYSEC-2025-183` ignore for PyJWT 2.12.1 because PyJWT 2.12.1 is the latest published version and no newer fixed release exists. Revisit when PyJWT publishes a newer release or the pip-audit advisory feed changes. | `.github/workflows/ci.yml` | mvp | medium | low | 9 | open |

<!-- P23 from /gabe-commit Phase 3 review-fixes (2026-05-13) — new route needs docs coverage -->
<!-- P24 from 2026-05-20 v2-dev.9 accepted-risk decision D44 — do not silently promote accepted minor-review risks without warning/order-preserving UX. Backend contract implemented 2026-05-20; row remains open for mobile/web warning UI. -->
<!-- P25 from 2026-05-20 Railway outage research and D46 — Render fallback is intentionally deferred until after production launch unless Railway blocks launch-critical proof again. -->
<!-- P26 from /gabe-push operational classifier (2026-05-20) — default-deferred PyJWT audit exception after CI config changed during P4 Phase 2 push. -->
| P27 | 2026-05-21 | gabe-review P4-Ph3 | `readApiError` helper duplicated identically in `transactions.ts:89` and `categories.ts:58` — two copies to maintain **RESOLVED (pre-existing, confirmed 2026-06-01):** readApiError is now a shared module (mobile/src/lib/apiError.ts) imported by both transactions.ts and categories.ts — no duplication remains. | `mobile/src/lib/categories.ts:58`, `mobile/src/lib/transactions.ts:89` | scale | low | negligible | 0 | resolved | 2026-06-01 |
| P28 | 2026-05-21 | gabe-review P4-Ph3 | `formatReviewLevel` uses `.replace("_", " ")` which only replaces first underscore — current values safe but fragile for future multi-underscore review levels | `mobile/src/screens/TransactionDetailScreen.tsx:569` | scale | low | negligible | 0 | open |
| P29 | 2026-05-21 | gabe-review P4-Ph4 | After Phase 4 commit/deploy, rerun the S23 push-registration Maestro flow against the deployed Railway staging API with migration 014 applied; local-stub proof is enough for pre-commit review but does not prove deployed route/migration/auth wiring. | `tests/mobile/maestro/p4-phase4-signout-push-active.yaml`, `backend/alembic/versions/014_mobile_push_tokens.py` | ent | high | high | 0 | resolved | 2026-05-24 |

| P30 | 2026-05-24 | gabe-review P4-Ph4 | `scripts/ops/cost_snapshot.py` is 865 lines — exceeds 800-line file org guideline. Standalone ops utility; could be split into modules for maintainability. | `scripts/ops/cost_snapshot.py` | scale | low | negligible | 0 | open |
| P31 | 2026-05-24 | user decision D47 | iOS runtime testing is officially deferred until after the full roadmap is implemented. Current P4/Phase 5 runtime gates are Android/S23-only; revisit iOS simulator/device proof post-roadmap. | `.kdbp/ROADMAP.md`, `.kdbp/PLAN.md`, `mobile/TESTING.md` | ent | low | moderate | 0 | open |

<!-- P27-P28 from /gabe-review Phase 3 (2026-05-21) — Scale-gate items deferred per triage option [1]. -->
<!-- P29 from /gabe-review Phase 4 (2026-05-21) — close during /gabe-push after deployed staging evidence is captured. -->
<!-- P30 from /gabe-review Phase 4 cross-agent consolidation (2026-05-24) — Scale-gate file-length item deferred per triage option [2]. -->
<!-- P31 from user decision D47 (2026-05-24) — iOS runtime lane deferred post-roadmap; Android/S23 remains the current mobile proof lane. -->
| P32 | 2026-05-25 | gabe-review P5-Ph1 | RLS tested statically only — migration content test checks strings; no test executes PostgreSQL RLS policy SQL for new statement tables. Requires PG-backed test harness or integration test env. | `backend/tests/test_statement_models.py:134` | ent | medium | moderate | 1 | resolved |
<!-- P32 resolved (verified 2026-06-07): the live-Postgres RLS proof already exists and RUNS in CI. ci.yml stands up a postgres:16 service + sets GASTIFY_TEST_PG_DSN so two PG-executed suites run (not skipped): (1) `backend/tests/test_rls_postgres.py` — direct-policy isolation + the subquery child-table shape (the statement_lines/verdicts pattern) + WITH CHECK cross-scope-insert block + FORCE ROW LEVEL SECURITY under a NON-superuser role + fail-safe-on-unset-GUC; (2) `backend/tests/test_group_isolation.py` — mirrors the REAL migration DDL (028 membership oracle, 003/027 transactions RLS, 029 cross-scope invite) against live PG. Both passed in CI 27095573766. Added in 8fbe921 after this row was filed → the "static only" premise was stale. Residual (LOW, for the P16 compliance audit): the suites COPY the policy DDL with a "keep in sync with migration NNN" comment rather than executing the literal alembic migrations end-to-end; a future strengthening could run `alembic upgrade head` against the PG test DB and assert isolation on the literal tables. -->

| P33 | 2026-05-25 | gabe-review P5-Ph1 | Subquery-based RLS on `statement_lines` and `statement_reconciliation_verdicts` (no direct `ownership_scope_id`) — correct pattern at ent but degrades at scale with large datasets. Fix: denormalize `ownership_scope_id` onto child tables. | `backend/alembic/versions/015_statement_reconciliation_foundation.py:349` | scale | low | moderate | 1 | open |
<!-- P33 stays deferred — it is a SCALE-tier PERFORMANCE item, not a correctness gap: the subquery child-table policy is proven CORRECT against live PG (see [[P32]] / test_rls_postgres.py test_subquery_policy_isolates_child_rows_across_scopes). Only revisit when statement_lines volume makes the per-row parent subquery a measured hotspot; at mvp the join is cheap. Do NOT denormalize pre-emptively (extra write-path column + backfill migration for no current benefit). -->


<!-- P32-P33 from /gabe-review P5 Phase 1 cross-agent consolidation (2026-05-25) — deferred per triage option [2] (fix MVP+Enterprise). P32 Enterprise gate, P33 Scale gate. -->
<!-- Backend P1 pending items are tracked inside .kdbp/archive/queued_backend-p1.md and activate with that plan. -->

| P34 | 2026-05-29 | roadmap-drive runtime defer | P6 Phase 3 item-flag persistence closed Exec on local backend evidence (666 tests). Deployed-staging runtime proof — branch-backed Railway staging with migration 022 + `PUT /transactions/{id}/items/{item_id}/flags` deployed, verified via `scripts/staging/run-insights-api-gate.py` against the staging URL. **RESOLVED 2026-06-01 (git 1a018b1):** gate PASSED on BOTH deployed backends (gastify-api-staging-e2e + gastify-api-staging) — signed in via Firebase, seeded 15 P6 fixture txns through the live API, verified insights rollups (5 top txn cats, 5 top item cats, 3 gravity centers), `PUT .../flags special_case` persisted (flag_mutation_verified=true), re-verified flag in tx detail + insights exclusion (excluded item 2×, $800 dropped; total $2,315). 7 artifact files written to tests/mobile/results/.../p6-insights-api-gate/. P6 Phase-6 staging exit gate satisfied. | `scripts/staging/run-insights-api-gate.py`, `backend/alembic/versions/022_transaction_item_user_flags.py` | ent | high | high | 0 | resolved | 2026-06-01 |

<!-- P34 from 2026-05-29 roadmap drive — user authorized "code-complete + local gates, defer deployed-staging/device/cutover runtime proof to PENDING" across P6→P9. Runtime evidence deferrals for this drive are tracked here and fold into the P6 Phase 6 staging exit gate / P7 launch hardening. -->
| P35 | 2026-05-29 | roadmap-drive android defer | Samsung S23 device e2e for the P6 Android insights + flag journey (Phase 5) is DEFERRED per explicit user direction ("we can defer android tests for later"). Android code + local typecheck/Jest are in scope; the S23 staging-e2e run with grouped stage artifacts is not run this drive. **RESOLVED 2026-06-01:** S23 device P6 insights + item-flag journeys ran GREEN last session (p6-insights-active, p6-item-flag-active on SM-S911B vs deployed staging-e2e); see [[s23-device-testing-wireless-adb]]. Plus the P34 API-level insights+flag gate passed on both deployed backends. | `tests/mobile/maestro/*`, `mobile/src/screens/*` | ent | high | high | 0 | resolved | 2026-06-01 |
| P36 | 2026-05-29 | gabe-review P7-Ph5 | Billing primitives are concurrency-naive (acceptable while not enforced): `get_or_create_balance` select-then-insert TOCTOU races on unique(ownership_scope_id); `deduct_scan_credit` read-check-decrement is non-atomic across sessions (could go negative). MUST be hardened (INSERT…ON CONFLICT + atomic `UPDATE…WHERE scan_credits>0`) before the deferred pricing-enforcement ADR wires these into live concurrent paths. | `backend/app/services/billing.py` | ent | medium | moderate | 0 | open |

<!-- P36 RESOLVED 2026-06-10 (P16 Phase 4): deduct_scan_credit is now an atomic UPDATE...WHERE scan_credits>0 RETURNING (no double-spend/negative); get_or_create_balance uses INSERT...ON CONFLICT DO NOTHING (no TOCTOU). Enforcement wired into scan submit (402), gated by billing_enforcement_enabled (default off). Proven: test_billing_postgres concurrency (N concurrent → row=max(0,K-N)); test_scans enforcement (exhausted→402). -->
<!-- P34/P35 runtime + P36 enforcement deferrals span the P6→P9 code-complete drive. -->

| P37 | 2026-05-29 | gabe-review P9-Ph1 | DP cohort `member_count` (n) is released EXACT (un-noised). The cohort sum/mean is ε-DP, but the count query (sensitivity 1) is protected only by the k≥20 floor — under frequent/repeated cohort queries an adversary could observe ±1 join/leave changes. Harden before cohort queries become frequent: noise the count with a separate ε budget (split ε_sum + ε_count ≤ 1) or release a bucketed count. Documented inline in CohortStat. | `backend/app/services/cohort.py` | scale | medium | moderate | 0 | open |
| P38 | 2026-05-30 | gabe-review P0-Ph1 | `ProgressFallback.start()` is not idempotent — a double-start without an intervening stop() would leak the prior AppState subscription + pending poll timer. Not reachable today (the hook effect always stop()s before re-start), so deferred. Mirror stop()'s cleanup at the top of start() if a future caller can double-start (e.g. StrictMode/hot-reload on the same instance). | `mobile/src/lib/progressFallback.ts` | mvp | low | low | 0 | open |
| P39 | 2026-05-30 | gabe-review P0-Ph1 | `GET /scans/{id}` `error_message` can echo provider (Gemini) response-body fragments (built by `scan_worker._fail_scan` from classified-error text). No NEW trust boundary — the same owner already receives this via the `scan_failed` WS/SSE event — so deferred. Forward hardening: redact/whitelist provider body fragments before persisting `error_message`. | `backend/app/services/scan_worker.py` | mvp | low | low | 0 | open |
| P40 | 2026-05-30 | roadmap-drive android defer | Phase-0 mobile polling-fallback S23 device runtime proof. **SCAN FALLBACK PROVEN ON-DEVICE 2026-06-01:** `scan-upload:{happy,review,failure}:active` all GREEN on S23 (SM-S911B, Android 16) against deployed staging-e2e — WS 403s, fallback polls the now-live `GET /scans/{id}`, scan-result-panel renders. Required a chain of fixes: defusedxml dep hotfix (1b7612f, container was crash-looping), GitHub-connecting the staging API services (they were CLI-deployed + stale, missing GET /scans/{id}), wireless-ADB lane (see [[s23-device-testing-wireless-adb]]), fixture seeding + clean-start. **NOW ALL GREEN 2026-06-01 (131a564):** golden-journey (scan→result→view-transaction via persisted transaction_id→edit) + statement-reconciliation both pass on-device too — fixes were fixture-seeding for golden, `scrollUntilVisible` for the below-fold scan-capture-panel, and dropping the brittle/stale data-dependent App-only assertion (`"No statement row"`). **P40 FULLY PROVEN on-device** (scan happy/review/failure + golden-journey + statement, all GREEN on S23 vs deployed staging-e2e with WS 403ing). | `tests/mobile/maestro/*`, `mobile/src/lib/progressFallback.ts` | mvp | high | moderate | 0 | resolved | 2026-06-01 |

<!-- P34/P35/P36 from 2026-05-29 roadmap drive. P37 from /gabe-review P9 Phase 1 — count-DP is a Scale-tier hardening; one-shot baseline risk is low (k≥20 + infrequent consent changes), so deferred with an inline caveat rather than degrading baseline accuracy with a noisy denominator now. -->
<!-- P37 from 2026-05-29 P9 review. -->

| P41 | 2026-05-31 | legacy-config cleanup / GitGuardian #33524280 | Restrict/rotate the gastify-staging Firebase WEB API key after the d7ea8fe legacy-config cleanup. **RESOLVED 2026-06-01:** owner rotated the key (new `AIzaSyB9H9…` propagated to gitignored `web/.env.staging-e2e` + Railway `gastify-web-staging` via CLI + validated by web e2e 2/2), **revoked the old `AIzaSyDA7…` key**, and **resolved GitGuardian #33524280**. Residual (optional, non-blocking): confirm the new key carries HTTP-referrer + API restrictions and App Check — public web key, low risk. | GCP console (off-repo); Railway `gastify-web-staging` | ent | medium | moderate | 0 | resolved | 2026-06-01 |

<!-- P41 from 2026-05-31 legacy-config cleanup (d7ea8fe). Owner off-repo action: GCP key restriction/rotation. GitGuardian #33524280 dashboard-resolved by owner; mirrored in memory project-firebase-key-followup. -->

| P42 | 2026-06-01 | S23 e2e coverage authoring | **FX provider does not support CLP→USD** — deployed staging logs show repeated `fx_fetch_failed ... CLP/USD ... 404 Not Found` from `https://api.frankfurter.dev/v1/rates/CLP/USD`. Frankfurter uses ECB reference rates, which do NOT include CLP (the app's PRIMARY currency), so REQ-18 (USD shadow on every transaction) is effectively broken — USD-shadow amounts aren't computed. Connects to the open "FX rate provider" ADR: pick a provider that supports CLP (e.g. open.er-api, exchangerate.host) before launch. **RESOLVED 2026-06-01 (82e8024):** swapped FX provider to open.er-api.com (prices CLP, free/no-key, `/v6/latest/{from}`→`rates[to]`); deployed to staging + staging-e2e via the git pipeline; verified a fresh CLP scan now persists `amount_usd_minor=368` + `fx_rate_to_usd=0.00112100` (3280 CLP ≈ $3.68); `p4-multicurrency-active` device flow GREEN (asserts Original/USD/FX). Note: USD shadow is snapshotted at create, so transactions scanned BEFORE the fix stay null. | `backend/app/services/fx.py`, `backend/app/config.py` (fx_api_url) | mvp | high | high | 0 | resolved | 2026-06-01 |

<!-- P42 from 2026-06-01 S23 device-flow authoring — discovered while assessing multi-currency e2e coverage; the FX provider (frankfurter) can't price CLP, breaking the USD-shadow value prop. -->



| P43 | 2026-06-01 | P32 RLS test discovery | **Deployed app connects to Postgres as the `postgres` SUPERUSER** (GASTIFY_DATABASE_URL on Railway = `postgresql://postgres@…/railway`). PostgreSQL superusers BYPASS row-level-security entirely (even with FORCE ROW LEVEL SECURITY), so the RLS tenant-isolation policies on every scope-bound table are SILENTLY INERT in production. NOT an active leak — the app also scopes every query in code (`.where(ownership_scope_id == auth.ownership_scope_id)`) and sets `app.ownership_scope_id` per request — so this is a defense-in-depth gap, not a data breach. Fix: create a dedicated non-superuser app role (LOGIN, table CRUD grants, NOT BYPASSRLS) on the Railway Postgres and point GASTIFY_DATABASE_URL at it; then RLS becomes a real second barrier. Proven mechanism: backend/tests/test_rls_postgres.py (passes against a non-superuser role; superuser bypasses). **CODE DONE 2026-06-01 (D67; mirrors Gustify D32) — awaiting per-env Railway var set + role provisioning (off-repo).** Final design (supersedes the earlier bootstrap approach): TWO non-superuser roles — `gastify_app` (runtime, non-owner, table CRUD → RLS applies) via GASTIFY_DATABASE_URL, and `gastify_migrator` (owns tables, runs DDL) via GASTIFY_MIGRATION_DATABASE_URL — PLUS a durable startup guard (`app/db.py::assert_least_privilege_role`, called from the FastAPI lifespan) that REFUSES TO BOOT if the runtime role is superuser/BYPASSRLS (skips local+SQLite). alembic/env.py routes to the migration URL + sets a placeholder app.ownership_scope_id GUC so FORCE-RLS FK-validation works as the non-bypassing migrator (a real bug the superuser was masking). PROVEN end-to-end vs a real Postgres: migrator owns 25 tables + migrates 001→026, app isolates a real transactions row across scopes, guard rejects a superuser DSN + passes gastify_app. REMAINING (operator, off-repo): provision the two roles (REASSIGN OWNED + grants) + set the two DSN vars on each Railway API service, then redeploy (the boot guard verifies). Runbook: docs/runbooks/db-role-split.md. | Railway API-service vars + role provisioning; `backend/app/db.py`, `app/main.py`, `config.py`, `alembic/env.py` | ent | high | high | 0 | resolved | 2026-06-02 |

<!-- P43 from 2026-06-01 — surfaced while writing the P32 PostgreSQL-executed RLS test: RLS only applies to non-superuser roles, and the deployed app uses the postgres superuser, so RLS is inert in prod (app-layer scoping still holds). Hardening, not an incident. -->

| P44 | 2026-06-03 | Phase 3 batch-scan review | **Batch-scan mvp test/UX gaps (e2e-covered, below mvp gate).** (1) Web batch presentational components (`BatchScanQueue.tsx`, `BatchScanReview.tsx`) have no vitest component tests — only Playwright `batch-scan.spec.ts`. (2) Mobile batch screens (`BatchCaptureScreen.tsx`, `BatchReviewScreen.tsx`) have no RNTL tests — only Maestro `p10-batch-scan-active`. (3) Mobile test-case catalog (LOCAL/STAGING_TEST_CASES) duplicated in `BatchCaptureScreen.tsx` from `HomeScreen.tsx` — extract a shared module to avoid drift if backend case IDs change. (4) Discard-failure leaves the item in `completed` with an errorCode and no discard-retry affordance. Orchestration logic + error paths ARE unit-tested (store/lib/hook, 15 web + 14 mobile) and both platforms have deployed staging-e2e proof, so these are presentational/maintainability gaps, not logic risk. | web/src/components/BatchScan*.tsx; mobile/src/screens/Batch*.tsx; mobile+web useBatchScan.ts | mvp | medium | low | 0 | open | |

| P45 | 2026-06-03 | gabe-commit | New `@router.get("/series")` decorator in `backend/app/api/insights.py` (insights time-series endpoint, D68) — no file in `docs/` updated. Same finding class as P23. Phase 4 feature docs land at phase completion (via `/gabe-teach`); endpoint contract already recorded in DECISIONS D68. | `backend/app/api/insights.py` | mvp | medium | moderate | 0 | resolved |
<!-- P45 resolved 2026-06-07 (with [[P49]]): documented /series in docs/runbooks/P6-INSIGHTS-CONTRACT.md § "Phase 4 Runtime API" (the granularity buckets, post-exclusion totals, zero-bucket gaps). -->

| P46 | 2026-06-03 | gabe-commit | `backend/app/services/insights.py` is 1066 lines (>800 cap). Pre-existing (was 909 before D68 added the series engine ~157 lines). Extract the series + gravity engines into `insights_series.py` / `insights_gravity.py` sharing the record loader + month helpers. Shape check is skipped at mvp, so non-blocking; surfaced for a focused refactor pass. | `backend/app/services/insights.py` | mvp | low | low | 0 | resolved |
<!-- P46 resolved 2026-06-07: insights.py (grown to 1667 lines with P66) split into a package `backend/app/services/insights/` — _shared.py (256, leaf primitives/dates/dataclasses/cache-key), loading.py (311, DB reads + cache), series.py (236), tree.py (489), monthly.py (524, rollups+gravity), __init__.py (re-exports the public surface so all 5 import sites are unchanged). All files <800. Done via an AST-based byte-exact splitter (no hand-retyping); verified by the full suite (832 passed — identical to pre-split), mypy 0, ruff clean. STRUCTURE.md gained `backend/app/services/**/*.py` for service sub-packages. -->


<!-- P44 from 2026-06-03 Phase 3 (batch scanning) /gabe-review — four below-mvp-gate findings (component/screen unit-test gaps #1-2 → Enterprise gate; catalog dedup #3 + discard-UX #4 → Scale gate). All e2e-covered; deferred per mvp maturity. -->
| P47 | 2026-06-03 | Phase 4 dashboard build | **Chart drill-down deferred (dimension toggle ships as mvp level-nav).** Phase 4 dashboard/trends donut has no (a) drill-to-transactions on slice click — `/transactions` has no `validateSearch`/URL filter schema, so deep-linking a category+date-range would require touching that route (out of Phase 4 scope); and (b) faithful hierarchical L1→L2 / L3→L4 in-chart drill — the API returns only top-5 L2/L4 leaves, so client-side parent grouping is lossy (D68). The store/item DimensionToggle covers the mvp level switch. Add when `/transactions` gains a search schema (drill-to-list) and/or the backend serves parent-rollup rows (faithful L1/L3). **UPDATE 2026-06-03 (e819b9c):** part (b) faithful hierarchical drill is SUPERSEDED by the D69 4-level cross-walk tree (`GET /insights/tree`) now powering the dashboard donut — drill is exact L1→L4 (store) / L3→L4 (item), not lossy client grouping. Residual = only part (a) drill-to-transactions, still gated on a `/transactions` `validateSearch` schema. **RESOLVED 2026-06-06 (Reports v2 Phase 1):** `/transactions/` now has `validateSearch {dateFrom,dateTo}` (web, seeds the filter from the URL) + the mobile `Transactions` route a `{dateFrom?,dateTo?}` param, and the report-detail "view transactions" drill deep-links the month-filtered list (web Playwright + S23 Maestro proven). The dashboard donut slice-drill can reuse the same schema next. | `web/src/routes/transactions.index.tsx`, `web/src/components/charts/CategoryDonut.tsx`, `mobile/src/screens/*` | mvp | low | low | 0 | resolved | 2026-06-06 |

<!-- P45-P46 from 2026-06-03 Phase 4 T1 /gabe-commit — P45 doc-drift (new /insights/series route, follows P23); P46 insights.py >800 (pre-existing, surfaced). Both deferred at mvp. -->
| P48 | 2026-06-03 | Phase 4 T7 proof | **Revisit Recharts 3 when es-toolkit ships ESM subpath exports.** Phase 4 pinned web charts to Recharts **2.15.4** (not 3.x) because Recharts 3's `es-toolkit` dep has CJS-only `./compat/*` subpath exports, which Vite's dev optimizer mis-shims (`require_isUnsafeProperty is not a function`) → dashboard crashes under `vite dev`. Not an active bug (2.15.4 works in dev+prod, React-19-compatible, identical API), but Recharts 3 has nicer combined-chart ergonomics. Re-evaluate when es-toolkit adds an `import` condition for `./compat/*`, or when a Recharts 3 + Vite workaround is confirmed. Also: correct PUSH.md `railway up` fallback env name `staging` → `staging-e2e`. | `web/package.json`, `web/vite.config.ts`, `.kdbp/PUSH.md` | mvp | low | low | 0 | open |

| P49 | 2026-06-03 | gabe-commit | New `@router.get("/tree")` decorator in `backend/app/api/insights.py` (insights drill-down tree endpoint, D69) — no file in `docs/` updated. Same finding class as P23/P45. Phase 4 feature docs land at phase completion (via `/gabe-teach`); endpoint contract already recorded in DECISIONS D69 + its 2026-06-03 amendment. | `backend/app/api/insights.py` | mvp | medium | moderate | 0 | resolved |
<!-- P49 resolved 2026-06-07 (with [[P45]]): documented /tree in docs/runbooks/P6-INSIGHTS-CONTRACT.md § "Phase 4 Runtime API" — the 4-level store cross-walk vs 2-level item tree, the D77 month/quarter/year period, and the P66 include_series sub-period sparkline series. -->

| P50 | 2026-06-03 | Phase 4 v2 user defer | **Mobile drill-down UI + S23 proof DEFERRED per user** ("defer S23 tests until I say; desktop web should be thorough"). Phase 4 v2 shipped + B2-proven on WEB only. Backend `/insights/tree` serves both; web+mobile api-types both regenerated. TODO when user greenlights S23: build the mobile recursive drill (mirror web `useDonutDrill` + `treeNodesToSlices`; gifted-charts `onPress(item,index)` + Pressable legend guarded on `!isOther`; screen-state path stack in DashboardScreen; Android BackHandler intercept) + EAS dev-client (react-native-svg already in) + run S23 Maestro drill flow vs deployed staging-e2e. **RESOLVED 2026-06-04 (same session, user greenlit S23):** mobile drill UI built (mirrors web — useInsightsTree + useDonutDrill + treeNodesToSlices + CategoryDonut onSlicePress + DrillBreadcrumb + Android BackHandler; NO EAS rebuild, JS-only over v1's svg/gifted-charts) + S23 Maestro proof GREEN (p10-dashboard-drill-active, 58s) vs deployed staging-e2e — full L1→L4 cross-walk + breadcrumb roll-up, screenshot-verified (LEDGER 2026-06-04). | `mobile/src/screens/DashboardScreen.tsx`, `mobile/src/components/charts/CategoryDonut.tsx`, `mobile/src/hooks/*`, `tests/mobile/maestro/*` | mvp | medium | moderate | 0 | resolved | 2026-06-04 |
| P51 | 2026-06-03 | Phase 4 v2 web proof | Statement-reconciliation WEB e2e tests (`capture-proof` "statement reconciliation buckets" + `statement-reconcile.spec.ts`) time out on `getByText("Coverage")` (90s) vs deployed staging-e2e. **Phase 5 statement flow, unrelated to Phase 4** — likely the fixture `scan_provider` doesn't yield statement-reconciliation data on staging-e2e, or the flow is slow/changed. Triage in a Phase 5 context (statement seed/provider, or skip-on-fixture-backend). | `tests/web-e2e/statement-reconcile.spec.ts`, `tests/web-e2e/capture-proof.spec.ts` | mvp | low | low | 0 | open |
| P52 | 2026-06-03 | deploy saga diagnosis | `backend/Dockerfile` installs deps via `uv pip install --system .` from **pyproject.toml (fresh resolve), NOT uv.lock** — so the deployed image can pull untested *latest* versions (drift from CI/tested deps; a newly-published version can break the build, as nearly happened with aiohttp this session). Harden for reproducible images: COPY `uv.lock` + install locked versions (`uv export --frozen` → `uv pip install -r`, or `uv sync --frozen`). NOTE: the actual Phase-4 deploy block was the Railway **Root Directory `/`→`backend`** misconfig (railpack mis-built the repo root as Node) — fixed operationally by the user; this Dockerfile item is a separate latent fragility. | `backend/Dockerfile` | mvp | medium | moderate | 0 | open |
| P53 | 2026-06-03 | Phase 5a review (python-reviewer) | D58-in-group **data** test deferred to 5c: assert each member's personal item-flags apply only to their OWN group view (per-viewer, not group-visible). 5a threads `user_id` unchanged through the scope-swap (documented in `resolve_analytics_scope`; group aggregates are unflagged because personal flags are RLS-invisible in group scope), but a faithful assertion needs group transactions + flags, which arrive with **5c share-to-group**. Add `test_personal_flags_apply_only_to_own_group_view` then. | `backend/tests/test_groups.py` (5c) | ent | medium | moderate | 0 | open |
| P54 | 2026-06-03 | Phase 5 plan (D70) | Phase 5 **mobile** group UI (`mobile/src/screens/GroupsScreen.tsx` + mobile scope switcher) + the S23 cross-group isolation Maestro proof are DEFERRED until the user reconnects the device (per user direction 2026-06-03, mirrors Phase 4 v2). Web Playwright + the Postgres-gated `test_group_isolation.py` are the primary Phase 5 isolation proofs meanwhile. Land mobile parity + S23 proof on greenlight. | `mobile/src/screens/GroupsScreen.tsx`, `tests/mobile/maestro/` | ent | medium | moderate | 0 | resolved | 2026-06-04 — S23 reconnected; p5-phase5-groups Maestro PASSED 37s on deployed staging-e2e (SM-S911B, Android 16): create→scope-switch→group banner→back to Personal |
| P55 | 2026-06-03 | Phase 5a /gabe-commit | `docs/architecture.md` narrative doc-drift: the group scope-swap (D70), the `app_is_scope_member` SECURITY DEFINER oracle, `scope_type='group'` + `ownership_scopes.name`, and the optional `group_id` param on insights monthly/series/tree are recorded in `.kdbp/DECISIONS.md` (D70) but not yet in `docs/architecture.md` Data Model / API Endpoints. Deferred at mvp (same class as P49 /insights/tree). Update when the Phase 5 group surface stabilizes (post-5d). | `docs/architecture.md` | mvp | low | low | 0 | resolved | 2026-06-05 — housekeeping rewrite of `docs/architecture.md` now documents the D70 scope-swap (validate-then-swap invariant + request-flow mermaid), the `app_is_scope_member` oracle, `scope_type='group'` (data-model erDiagram + G3 section), and scope-aware insights `group_id`; deep detail in `docs/wells/3-identity-ownership.md`. |
| P56 | 2026-06-04 | Phase 5b review | No DB-level constraint enforces the one-owner-per-group invariant (app logic prevents multi-owner: create makes one owner, `RoleUpdate` is `Literal['admin','member']` so you can't promote to owner). A partial unique index `CREATE UNIQUE INDEX ... ON ownership_scope_members(ownership_scope_id) WHERE role='owner'` would be defense-in-depth — DEFERRED because it would constrain a future owner-transfer flow (demote+promote in one txn). Add with the owner-transfer feature. | `backend/alembic/versions/` | ent | low | moderate | 0 | open |
| P57 | 2026-06-04 | Phase 5b review | Minor 5b hardening bundle (all reviewer MEDIUM/LOW, none blocking): (a) `_is_scope_member` SQLite branch lacks a `scope_type='group'` filter — a caller passing their OWN personal scope id to a /groups/{id} route renders it as a group (own data, no cross-tenant leak); (b) `create_invite`/`join` use 200 not 201 (rotation/idempotent semantics — debatable); (c) `db.bind` is not a declared SQLAlchemy 2.x public attr (works in 2.0.49, used codebase-wide) → consider `get_bind()`; (d) `_is_postgres` duplicates the deps.py inline dialect check (consolidate to db.py); (e) `_resolve_membership` does 2 round-trips (oracle + role SELECT) — could fold role into the oracle. | `backend/app/api/groups.py`, `backend/app/auth/deps.py` | mvp | low | low | 0 | open |
| P58 | 2026-06-04 | Phase 5c review | No dedicated Postgres integration test for the share-to-group flow (read-personal → swap → write-group). The generic `test_rls_postgres.py` proves the WITH CHECK + USING policy SHAPE (synthetic tables), and the SQLite tests prove the app logic, but a PG test that exercises the real `transactions` WITH CHECK + the swap-timing (source invisible under the group GUC) on the actual schema would close the gap. Add a focused PG test (needs the real transactions/items tables under the non-bypassing role). | `backend/tests/test_group_isolation.py` | ent | low | moderate | 0 | open |
| P60 | 2026-06-04 | Phase 5d mobile | Mobile group UI parity gaps vs web (MVP shipped: scope switch via GroupsScreen, create/list/leave, share, dashboard scope banner; S23 Maestro proof). DEFERRED for parity: (a) scan-disabled in group mode (web has PersonalOnlyNotice on /scan + /scan-batch; mobile scanning is not yet guarded — D70 scan-personal-only); (b) a header scope switcher (web has a dropdown; mobile switches via the GroupsScreen "View dashboard"); (c) invite-link UI (generate/preview/join) + role-management UI (make/remove admin, remove member) — backend endpoints exist, web has the UI; (d) SecureStore persistence of activeScope (web persists to localStorage; mobile is in-memory). **2026-06-04 hardening: (a) scan-disable [HomeScreen + BatchCaptureScreen guarded in group scope + personal-only-notice] and (d) SecureStore persistence [scopeStore + hydrate-on-auth + reset-on-signout] RESOLVED. Remaining: (b) header scope switcher, (c) invite-link + role-management UI.** | `mobile/src/screens/GroupsScreen.tsx`, mobile scan screens | ent | low | moderate | 0 | open |
| P59 | 2026-06-04 | D71 adversarial review | No rate limiting on the invite endpoints (`GET /invites/{token}`, `POST /invites/{token}/join`) — or anywhere in the API. Invite token entropy is 192 bits (`secrets.token_urlsafe(24)`) so brute-force is infeasible, but bulk authenticated probing is unrestricted (DoS/abuse, defence-in-depth). Codebase-wide gap, not Phase-5-specific; add `slowapi` (per-user/IP) on the invite + auth-sensitive endpoints in a security-hardening pass. | `backend/app/main.py`, `backend/app/api/groups.py` | ent | low | moderate | 0 | resolved |
| P61 | 2026-06-04 | Phase 5 client review (workflow) | Deferred Phase 5 client-review findings after the 2026-06-04 hardening pass (error-handling, double-submit, D70 scan-guard, scope persistence, TrendsScreen banner all FIXED + re-proven on both platforms). Remaining low/non-blocking: **test-coverage** — (#1) no scope-isolation hook tests asserting group_id in insights params + scope in the query key (web + mobile); (#14) no web test for D70 group-mode scan enforcement (PersonalOnlyNotice); (#20) no GroupsScreen mobile tests (create/leave/cache-invalidation); (#22) no ScopeBanner group-render test. **polish** — (#3) no confirm dialog before destructive web delete-group/remove-member; (#9) useDeleteGroup leaves a (unreachable, self-GCing) stale detail cache entry — add `qc.removeQueries(groupKeys.detail(id))`; (#12) renaming the active group leaves a stale name in activeScope until reload (sync setActiveScope in useRenameGroup.onSuccess). | `web/src/hooks/useInsights.test.tsx`, `web/src/routes/scan.tsx`, `mobile/src/screens/GroupsScreen.tsx`, `web/src/hooks/useGroups.ts` | ent | low | moderate | 0 | resolved | 2026-06-04 (2c035f2 — 16 tests #1/#14/#20/#22 + polish #3/#9/#12) |
| P62 | 2026-06-04 | Phase 5 MVP ship | Phase 5 "5e" slice deferred at MVP ship: **consent-gated member detail** — group dashboards show aggregates by default (current MVP); showing other members' individual transactions is admin-configured (up to 3 admins) + per-user accept/decline consent (D70 product decision). Plus the **member-filtered transactions list** per D72 (departed contributors' shared txns stay in statistics but are hidden from the transactions list). Backend share/leave/delete + RLS scope-swap already shipped; this is the consent-config UI + the visibility filter on top. Web + mobile + S23 proof when built. | `backend/app/api/groups.py`, `web/src/routes/groups.tsx`, `mobile/src/screens/GroupsScreen.tsx` | ent | medium | moderate | 0 | resolved | 2026-06-04 (5e shipped: D73 + migration 032 + web/mobile UI, authz-reviewed, B2-proven, promoted P50) |

<!-- P47 from 2026-06-03 Phase 4 web build — chart drill-down deferred (drill-to-list needs /transactions search schema; hierarchical L1/L3 needs backend parent rollups). Dimension toggle is the mvp level-nav. UPDATE 2026-06-03: part (b) faithful drill superseded by D69 /insights/tree (e819b9c); only (a) drill-to-list remains. -->
<!-- P48 from 2026-06-03 Phase 4 T7 — Recharts pinned to 2.x (es-toolkit/Vite-dev incompat on v3); revisit v3 later. Non-blocking. -->
<!-- P49 from 2026-06-03 Phase 4 v2 /gabe-commit — new /insights/tree route doc-drift, deferred at mvp (same class as P23/P45). -->
| P63 | 2026-06-04 | Phase 5 5e review (workflow) | Deferred 5e review LOW nits (HIGH + 4 LOW fixed pre-promote): (#3) list_group_transactions builds the group-flag gate in a Python branch not the SQL WHERE — no live bug, add a self-documenting `and_(member_visibility_enabled, sharer.shares_detail)` form to harden against a future subquery refactor; (#4) no index on `ownership_scope_members(ownership_scope_id, shares_detail)` — fine at MVP scale, add when group sizes grow; (#7) GroupTransactionsSection renders an always-clickable toggle even when member_visibility_enabled is false — show a contextual 'aggregates only' note; (#8) an admin sees both VisibilitySection + ConsentControl — minor redundancy; (#11) mobile MemberRow role/remove callbacks could add an isPending early-return (already disabled-guarded on the Pressable). | `backend/app/api/groups.py`, `backend/alembic/versions/`, `web/src/routes/groups.tsx`, `mobile/src/screens/GroupDetailScreen.tsx` | ent | low | low | 0 | open |
| P64 | 2026-06-05 | device deferral (user) | S23 Maestro proof for Reports granularity (D77) DEFERRED — device offline. Re-run `p12-reports-active` and add a `reports-granularity-{week,quarter,year}` Maestro flow when the S23 reconnects. Shipped to prod (P58) with web Playwright proof + mobile jest (227); only the on-device Android proof is outstanding. | `tests/mobile/maestro/p12-reports-active.yaml` | mvp | low | low | 0 | resolved | 2026-06-05 — S23 reconnected (SM-S911B, wireless ADB 192.168.1.83:5555). Both flows GREEN vs deployed staging-e2e: `p12-reports-active` (34s, cards render) + NEW `p12-reports-granularity-active` (1m 1s, month→week→quarter→year→month). Week bucket shows real ISO-week spend (W20 $388.47, W21 $270.44/76 txns); year $7,283.91/94 txns; month May $658.91/79. Artifacts: tests/mobile/results/runs/staging-e2e/p64-reports-20260606T010823Z/. |
| P65 | 2026-06-05 | Phase 7 S23 proof (surfaced) | **Cross-screen RN warning: VirtualizedList nested in a plain ScrollView.** `ScreenShell` wraps its children in a `ScrollView`, and the list screens (`ItemsScreen`, `NotificationsScreen`, and any other `<ScreenShell><FlatList/>`) put a FlatList inside it → the dev-only "VirtualizedLists should never be nested inside plain ScrollViews" warning (breaks FlatList windowing; non-fatal, absent in production builds). PRE-EXISTING (shipped since Phase 6 Items), surfaced in the Phase 7 S23 proof. Fix is a cross-screen refactor (e.g. a `ScreenShell` variant that renders no outer ScrollView for list screens, or have list screens use SafeAreaView + the FlatList's own header/footer instead of ScreenShell). Out of Phase 7 scope; do as a list-screen polish pass. | `mobile/src/components/ScreenShell.tsx`, `mobile/src/screens/{Items,Notifications}Screen.tsx` | mvp | low | low | 0 | resolved |
<!-- P65 resolved 2026-06-07: ScreenShell gained a `scroll?: boolean` prop (default true). ItemsScreen + NotificationsScreen pass `scroll={false}` so their FlatList is the sole scroll container (it already routed all surrounding content through ListHeaderComponent/ListFooterComponent); padding the shell's ScrollView used to provide now lives on the FlatList's contentContainerStyle (SCREEN_PADDING). The FlatList is no longer nested in a plain ScrollView → the warning is structurally gone. Verified: tsc + biome clean, ItemsScreen + NotificationsScreen jest pass; the warning does NOT fire in the jest env (confirmed by experiment) so it can only be proven absent at RUNTIME — S23 logcat grep during an Items/Notifications render. -->

| P66 | 2026-06-06 | Reports v2 Phase 3 descope | **Per-category trend sparklines on the report-detail group cards.** Phase 3 shipped the quarter/year grouped breakdown (the substantive D77 lift) but descoped the legacy "Resumen" per-category trend indicator (a colored ▲/▼ % or mini-SVG sparkline per group card showing the category's spend vs the prior period). Cleanest path: a backend per-node prior-period diff — build the current + prior-period tree and attach `prior_total_minor` (or a `trend_pct`) to each `InsightsTreeNode`, then render the sparkline/colored-% in the web `GroupCard` + mobile `GroupCard` (port legacy `CategoryGroupCard`/`ItemGroupCard` TrendSparkline). Web + mobile + B2 proofs when built. The quarter/year breakdown + insight already shipped without it. | `backend/app/services/insights.py`, `web/src/components/reports/ReportDetailOverlay.tsx`, `mobile/src/screens/ReportDetailScreen.tsx` | mvp | low | low | 0 | resolved |
| P67 | 2026-06-07 | P16 T2 doc-drift | **Erasure docs reference the old anonymize-in-place behavior** — erasure is now HARD-DELETE (D89, amends D4). `P6-INSIGHTS-CONTRACT.md` fixed in T2; sweep the rest in the Phase-5 compliance audit and confirm each erasure mention says hard-delete + PII-free `dsr_erasure` audit event. | `docs/runbooks/SECURITY-CHECKLIST.md`, `docs/runbooks/P7-LAUNCH-EXIT-GATE.md`, `docs/rebuild/api/CROSS-CUTTING.md`, `docs/rebuild/api/OPENAPI-SKETCH.md`, `docs/rebuild/SCHEMA-SKETCH.md` | mvp | low | low | 0 | open |
| P68 | 2026-06-07 | P16 T5 FE follow-up | **Web/mobile leave-group "delete my shared data?" prompt.** The backend keep-vs-delete CHOICE is live (`POST /groups/{id}/leave?delete_shared=`, D82) + proven API-level (T5). The web + mobile leave flow still calls leave with the default (keep) — add a confirm dialog offering "keep my shared data in the group" vs "remove it (the group's stats for those months shut down)", wiring the boolean through `useLeaveGroup` / the mobile GroupsScreen leave action. i18n the copy (es/en/pt). Non-blocking for the P16 launch gate (the mechanism + API choice exist + are proven); ship with the next groups UI pass. | `web/src/hooks/useGroups.ts`, `web/src/routes/groups*.tsx`, `mobile/src/screens/GroupsScreen.tsx`, `web/src/lib/i18n.ts` | mvp | low | low | 0 | open |
| P69 | 2026-06-10 | gabe-review P16-P1 | **Void incompleteness in derived/multi-period insight surfaces.** (a) Gravity-center `baseline_average_minor` for a NON-voided month still averages over the trailing baseline window, which can include a tombstoned month → a derived figure reflects erased spend (`monthly.py` void check covers only `[normalized_period, range_end]`, not `db_start = shift_months(-_BASELINE_MONTHS)`). (b) Series quarter/year/week buckets only void when a tombstoned month lies INSIDE the requested range; a partially-requested bucket (or a week spilling outside the range) can show un-voided spend, disagreeing with `/monthly` for the same labelled period. Both are second-order (derived average / partial bucket), group-scope only. Fix: extend the void check to the baseline window (suppress gravity centers when it overlaps a tombstone) + void a series bucket whenever ANY month it spans is tombstoned (not just requested-range months). | `backend/app/services/insights/monthly.py:81`, `backend/app/services/insights/series.py:96` | ent | medium | moderate | 0 | open |
| P70 | 2026-06-10 | gabe-review P16-P1 | **Void/erasure edge test-coverage gaps** (the live-PG erasure flow + void edges SQLite can't prove). Add: (1) a live-PG (`test_group_isolation.py`-style) erasure-flow test asserting the membership row is actually GONE after account-delete (the R1 bug was invisible to SQLite + the Python-counter staging assert); (2) `_apply_series_void` with week granularity (month-boundary buckets) + quarter/year buckets; (3) `months_in_range` Dec→Jan rollover; (4) `void_reason_for` mixed-reason precedence (both reasons in one range); (5) `tombstone_member_shares` overlap branch (month already tombstoned by another member). | `backend/tests/test_group_isolation.py`, `backend/tests/test_group_stat_tombstone.py` | ent | medium | moderate | 0 | resolved |
| P71 | 2026-06-10 | gabe-review P16-P1 | **Admin member-removal forecloses the removed member's D82 keep-vs-delete choice.** `DELETE /{group_id}/members/{member_user_id}` (admin removal) always KEEPS the removed member's shared data (D72) with no later path for that member to void it — only the member's OWN `leave?delete_shared` offers the choice, which they can no longer reach once removed. Defensible (admins shouldn't delete a member's data on their behalf), but a removed member has no recourse to exercise erasure of their group shares short of full account deletion. Consider a post-removal self-service "void my shares in groups I was removed from" path, or surface this in the privacy UI. | `backend/app/api/groups.py:491` | ent | medium | moderate | 0 | open |
| P72 | 2026-06-10 | gabe-review P16-P1 | **Portability/data-access omit the user's group-shared copies.** `GET /privacy/portability` + `/data-access` export only personal-scope transactions; the content the user shared INTO groups (`shared_by_user_id == user_id`, living in group scopes) is not exported. Once the personal source is deleted, that shared content is entirely unexportable. GDPR Art 20 (portability) arguably covers data the user provided to the group. Add the user's group-shared copies (cross-scope, via the `app_user_groups` reader + per-group GUC reads, mirroring `_void_user_group_shares`) to the portability export. | `backend/app/api/privacy.py:304` | ent | medium | moderate | 0 | open |
| P74 | 2026-06-10 | P16 P2 validation | **consent_records keep-forever TTL asymmetry → Phase-5 ratify.** There is NO consent-record purge anywhere — revoked/PII-scrubbed rows live forever, while audit proof expires at ~6y. Keep-forever is defensible (the rows are PII-free and ARE the GDPR Art 7(1)/Law 21.719 consent proof), but the asymmetry must be an explicit decision, not silent. Ratify either keep-forever (DECISIONS entry) OR a bounded TTL aligned to the DSR-proof/limitation window. | `backend/app/services/retention.py`, `.kdbp/DECISIONS.md` | ent | low | low | 0 | open |
| P75 | 2026-06-10 | P16 P2 validation | **Re-identification residue after anonymization → Phase-5 decide.** `anonymize_user_profile` nulls only email + display_name; `users.firebase_uid` (a stable external re-identification vector), default_currency, locale survive on the scrubbed shell. Decide vs GDPR Recital 26 (anonymous only if re-identification no longer reasonably possible) whether to also scrub firebase_uid, and document the engineering rationale (it is the anchor for retained proof rows + a re-sign-in correctly JIT-provisions a fresh scope). | `backend/app/services/consent.py:439` | ent | medium | moderate | 0 | open |
| P76 | 2026-06-10 | P16 P2 validation | **DSR proof-retention window → Phase-5 bound it.** Migration 037 EXEMPTS dsr_* events from the generic 6y purge (so the dsr_erasure proof survives), but the exact proof-of-DSR window is now unbounded-by-this-job. Decide the limitation-period-aligned window per regime and bound the "we can prove we honored erasure" claim explicitly in the D88 attestation (complaint limitation periods can exceed 6y or be open-ended). | `backend/app/services/retention.py:45` | ent | low | moderate | 0 | open |
| P77 | 2026-06-10 | P16 P2 validation | **Cohort OUTPUT consumer is P9-deferred.** Exit-signal-(b) is proven at the eligibility roster AND now at the SQLite cohort-aggregate output (`test_revoked_user_contribution_absent_from_cohort_output`), but no DEPLOYED consumer wires `eligible_cohort_member_ids` → contributions → `cohort_baseline` (cohort.py is decoupled; the join is P9/P18 machinery). When the deployed cohort run ships, add an explicit fix-or-accept asserting a revoked user's spend never reaches a released CohortStat (adjacent to P37). | `backend/app/services/cohort.py:137` | ent | medium | moderate | 0 | open |
| P78 | 2026-06-10 | P16 P2 validation | **audit_events "append-only" claim vs reality → Phase-5 reconcile.** `004_consent_dsr.py:101` comments the table "append-only at Ent tier" and SCHEMA-SKETCH.md documents a BEFORE UPDATE/DELETE trigger, but no such trigger exists — and can't as-is, because `scrub_user_audit_trail` UPDATEs ip_address and the purge DELETEs rows. Either implement a trigger permitting ONLY those two governed mutations (mirror migration 036's tombstone append-only pattern) or correct the immutability claim, so the D88 matrix doesn't assert a guarantee the system lacks. | `backend/alembic/versions/004_consent_dsr.py:101`, `docs/rebuild/SCHEMA-SKETCH.md` | ent | medium | moderate | 0 | resolved |
| P79 | 2026-06-10 | P16 P2 validation | **Processing-register declared TTLs vs enforced retention → Phase-5 reconcile.** The register declares per-purpose windows ("account + 1y" analytics, "account + 7y" receipt_scanning) but the runner enforces ONLY scans-90d + audit-6y; the rest are account-lifetime/erasure-deleted, not runner-purged. Reconcile the GDPR Art-30 / CPRA-retention-disclosure record with actual behavior (a false declared period is itself a violation); seed a test that the declared strings match enforced reality. | `backend/alembic/versions/004_consent_dsr.py:166`, `backend/app/services/retention.py` | ent | medium | moderate | 0 | open |
<!-- P66 resolved 2026-06-06: shipped REAL within-period sparklines (user upgraded the scope from the legacy direction-only indicator to a true time-series). Backend `/insights/tree?include_series=true` attaches a per-root sub-period series (month→weeks, quarter/year→months) reusing the series bucketing; web + mobile `Sparkline` render it on group headers (up=red/down=green/flat=neutral). Adversarially reviewed (22 confirmed, mostly correctness confirmations; HIGH week-boundary hardened with a period clamp; year-granularity test added). backend 832 / web 122 / mobile 250. -->
| P80 | 2026-06-10 | prod web outage fix | **CI builds `frontend/` but production deploys `web/`** — two different frontend apps. The web prod outage went uncaught because CI's Frontend jobs (`working-directory: frontend`, `npm ci`) never validate `web/` — its `package-lock.json` had silently drifted (`@emnapi/core` missing). Reconcile: point CI at `web/` (or retire whichever of `frontend/`/`web/` is stale) + add a build-the-deployed-web CI job so lockfile/Rolldown-binding issues fail CI, not prod. Also `web/` now builds via web/Dockerfile; `web/nixpacks.toml` is dead config worth removing. | `.github/workflows/ci.yml`, `web/`, `frontend/` | mvp | medium | moderate | 0 | open |
| P81 | 2026-06-10 | CI repoint to web/ | **web/ lint debt (6 errors) + 0 tests.** Repointing CI at the deployed web/ (P80) surfaced 6 eslint ERRORS — conditional `useEffect` (react-hooks/rules-of-hooks ×2, potential real bugs), refs-during-render, setState-in-effect, 2 unused vars — so the Web Lint CI job is `continue-on-error: true` (non-blocking) for now. Also web/ has ZERO test files (Web Test is a hollow green). Fix the 6 lint errors → drop continue-on-error; add real web/ component tests. | `web/` | mvp | medium | moderate | 0 | resolved |
| P82 | 2026-06-10 | web-e2e sweep | **Group-creating E2E specs never clean up — MAX_GROUPS cap fills and 409s future runs.** groups.spec/groups-multiuser/groups-hardening each create timestamped groups on staging-e2e and leave them; once the e2e user hits MAX_GROUPS_PER_USER, group-creation specs fail 409 (happened 2026-06-10; 5 stale groups purged by hand). Add per-spec cleanup (delete the group in afterEach via the API) or a pre-run sweep script. | `tests/web-e2e/groups*.spec.ts` | mvp | low | low | 0 | resolved |
